# ОДУ — Билеты по курсу ОДУ

> **Литература:** Ю.Н. Бибиков «ОДУ», А.Ф. Филиппов «Лекции по ОДУ», Романко «Разностные уравнения»

---

## Вводное слово: что такое ДУ и зачем

Дифференциальное уравнение — это уравнение, которое связывает независимую переменную $x$, неизвестную функцию $y(x)$ и её производные. Главный вопрос всего курса: **существует ли решение, единственно ли оно, и как его найти?**

Курс идёт от конкретных методов решения (Глава 1) к общим теоремам существования и единственности (Глава 2). Сначала мы учимся решать конкретные типы уравнений — с разделяющимися переменными, линейные, в полных дифференциалах. Затем доказываем, что решения вообще существуют (теорема Пикара) и как долго они живут (теория продолжимости).

---

## Билет 1. Понятие ДУ 1-го порядка. Примеры. Математические модели. Дискретные модели и разностные уравнения. Основные определения

### Что такое ДУ

**ДУ $n$-го порядка** — уравнение вида:
$$F\bigl(x,\, y(x),\, y'(x),\, \ldots,\, y^{(n)}(x)\bigr) = 0$$
где $x$ — независимая переменная, $y(x)$ — неизвестная функция.

**Порядок ДУ** — порядок старшей производной.

**Решить ДУ** — найти $y(x)$.

Идеологически ДУ — это то, что определяет $y^{(n)}(x)$ как «хорошую» функцию от остальных переменных:
$$y^{(n)}(x) = g\bigl(x,\, y,\, y',\, \ldots,\, y^{(n-1)}\bigr)$$
Это называется **ДУ, разрешённое относительно старшей производной**.

*Почему «слишком общее» определение не работает:*
- $x + y + 0 \cdot y' = 0$ — формально похоже на ДУ, но производной нет
- $y' - |y'| = 0 \iff y' \geq 0$ — это неравенство, а не уравнение

---

### Примеры и математические модели

**2-й закон Ньютона:**
$$ma = F \iff m\ddot{x} = F(x, \dot{x}) \quad \text{— ДУ 2-го порядка}$$
где $v = \dfrac{dx}{dt}$ — скорость, $a = \dot{v} = \ddot{x}$ — ускорение.

**Задача Дебона (1635).** Найти кривую, у которой в каждой точке горизонтальный катет треугольника, образованного касательной, ординатой и осью $Ox$, постоянен и равен $c$.

Из геометрии: $\operatorname{tg}\alpha = y'(x) = \dfrac{y(x)}{c}$

Решение: $y = A e^{x/c}$

**Радиоактивный распад.** Дан период полураспада $T$: $m(t+T) = \dfrac{1}{2}m(t)$.

---

### Дискретные модели и разностные уравнения

**Дискретная модель** распада: разобьём время на шаги $t_k = t_0 + kT$, $m_k = m(t_k)$:
$$\boxed{m_{k+1} = \frac{1}{2} m_k} \quad \text{— разностное уравнение}$$

Решение: $m_k = \dfrac{m_0}{2^k}$. Но это даёт $m(t)$ только в моменты $t = t_0 + kT$.

Для **непрерывной модели** перейдём к пределу при $\Delta t \to 0$:
$$\frac{\Delta m(t)}{\Delta t} = km(t) + \frac{o(\Delta t)}{\Delta t} \xrightarrow{\Delta t \to 0} \dot{m}(t) = k\,m(t)$$

$$\boxed{\dot{m}(t) = k\,m(t)} \quad \text{— ДУ радиоактивного распада}$$

Решение при $m(t_0) = m_0$: $m(t) = m_0\,e^{k(t-t_0)}$

---

### Основные определения

Пусть $f \in C(G)$, $G$ — связная область в $\mathbb{R}^2$. Рассматриваем ДУ:
$$y' = f(x, y) \tag{1}$$

> [!def] Решение ДУ
> Функция $y = \varphi(x)$ на $\langle a,b \rangle$ называется **решением ДУ (1)**, если:
> 1. $\exists\,\varphi'(x)$ для $\forall\,x \in \langle a,b \rangle$
> 2. $\operatorname{Graf}\varphi \subset G$, т.е. $(x, \varphi(x)) \in G$ для $\forall\,x$
> 3. $\varphi'(x) = f(x, \varphi(x))$ на $\langle a,b \rangle$

График решения называется **интегральной кривой** ДУ.

> [!def] Задача Коши (ЗК)
> Пусть $(x_0, y_0) \in G$. ЗК — найти решение ДУ (1), график которого проходит через $(x_0, y_0)$:
> $$\begin{cases} y' = f(x, y) \\ y(x_0) = y_0 \end{cases}$$
> Точка $(x_0, y_0)$ — **начальная точка**, пара $(x_0, y_0)$ — **начальные данные**.

> [!def] Точка единственности
> Точка $(x_0, y_0)$ называется **точкой единственности (м. !-мой)**, если у ЗК $\exists$ решение $y = \varphi(x)$ на $\langle a,b \rangle \ni x_0$, такое что любое другое решение $\Psi(x)$ на $\langle a_1,b_1 \rangle \ni x_0$ совпадает с ним:
> $$\Psi \equiv \varphi \quad \text{на } \langle a,b \rangle \cap \langle a_1, b_1 \rangle$$

---

## Билет 2. Уравнения с разделяющимися переменными. Теоремы об общем решении (Т1 и Т2)

### УРП — общий вид

Самое базовое ДУ — задача о первообразной: $y' = f(x) \Rightarrow y = \int f(x)\,dx + C$.

**УРП (уравнение с разделяющимися переменными):**
$$y' = f(x)\,g(y) \tag{1}$$

Пусть $f \in C(\langle a,b \rangle)$, $g \in C(\langle \alpha,\beta \rangle)$, $G = \langle a,b \rangle \times \langle \alpha,\beta \rangle$.

*(Якоб Бернулли, 1695, «integralis»)*

**Неформальный рецепт** (при $g(y) \neq 0$):
$$\frac{dy}{g(y)} = f(x)\,dx \implies \int \frac{dy}{g(y)} = \int f(x)\,dx \implies \boxed{H(y) = F(x) + C} \tag{2}$$

где $F$ — первообразная $f$, $H$ — первообразная $\dfrac{1}{g}$.

> [!warning] Даже если $H(y)$ задана явно, написать $H^{-1}$ явно не всегда возможно. Пример: $y' = \dfrac{y^2+1}{y^2+2}$ даёт $y + \arctan y = x + C$.

---

### Теорема 1 (случай $g(y) \neq 0$)

> [!theorem] Теорема 1
> Пусть $f \in C(\langle a,b \rangle)$, $g \in C(\langle \alpha,\beta \rangle)$, $g(y) \neq 0$ на $\langle \alpha,\beta \rangle$. Тогда:
> 1. Формула (2) задаёт все решения (1) и только их (это **общее решение**)
> 2. $G = \langle a,b \rangle \times \langle \alpha,\beta \rangle$ — область $\exists!$: через каждую точку $(x_0,y_0) \in G$ проходит единственная инт. кривая:
> $$\int_{y_0}^{y} \frac{dt}{g(t)} = \int_{x_0}^{x} f(s)\,ds \tag{4}$$

**Доказательство п.1.** $(1) \Leftrightarrow (2)$? Пусть $y = \varphi(x)$ — решение (1): $\varphi'(x) = f(x)g(\varphi(x))$.
$$\frac{\varphi'(x)}{g(\varphi(x))} = f(x) \iff \frac{d}{dx}H(\varphi(x)) = \frac{d}{dx}F(x) \iff H(\varphi(x)) = F(x) + C$$
Поскольку $\dfrac{d}{dx}H(\varphi(x)) = H'(\varphi(x))\cdot\varphi'(x) = \dfrac{1}{g(\varphi(x))}\varphi'(x)$ — это в точности производная $F(x)$. $\Rightarrow \varphi(x)$ задаётся (2).

**Доказательство п.2.** По п.1 все решения содержатся в (2). Подставим нач. усл.: $H(y_0) = F(x_0) + C \Rightarrow C = H(y_0) - F(x_0)$ — единственно. Подставляя, получаем (4). $\blacksquare$

---

### Случай 2: $g(y)$ имеет корни. Особые решения

Пример Пеано: $y' = 3y^{2/3}$, $(0,0)$, $y(0) = 0$.

При $y \neq 0$: $\int \frac{dy}{3y^{2/3}} = \int dx \Rightarrow y^{1/3} = x + C \Rightarrow y = (x+C)^3$.

Также $y = 0$ — решение. Склеенные решения:
$$y = \begin{cases} 0, & x < c \\ (x-c)^3, & x \geq c \end{cases}$$

В каждой точке $(x_0, 0)$ — бесконечно много решений $\Rightarrow$ точка **не** является точкой единственности.

> [!def] Особое решение
> Решение называется **особым**, если в каждой его точке нет единственности.

Пусть $a$ — корень $g(a) = 0$. Тогда $y = a$ — **стационарное решение**.

Общий ответ (кандидат):
$$\begin{cases} H(y) = F(x) + C & \text{где } g(y) \neq 0 \\ y = a, & g(a) = 0 \end{cases} \tag{5}$$

> [!theorem] Теорема 2
> Пусть $f \in C(\langle a,b \rangle)$, $g \in C^1(\langle \alpha,\beta \rangle)$. Тогда:
> 1. Формула (5) задаёт все решения (1) и только их
> 2. $G = \langle a,b \rangle \times \langle \alpha,\beta \rangle$ — область $\exists!$

*(Т2 следует из общей теоремы Пикара)* $\blacksquare$

---

### Примеры

**Пример 1. Модель взрыва:** $y' = y^2$, $y(0) = 1$.

$$\frac{dy}{y^2} = dx \Rightarrow -\frac{1}{y} = x - C \Rightarrow y = \frac{1}{C-x}$$

Из $y(0)=1$: $C=1$. Решение: $y = \dfrac{1}{1-x}$, $x \in (-\infty, 1)$. Уходит на $+\infty$ за конечное время.

**Пример 2. Экспоненциальный рост/убывание** (радиоактивный распад, бактерии, эпидемия):
$$y' = ky \Rightarrow \frac{dy}{y} = k\,dx \Rightarrow \ln|y| = kx + c \Rightarrow \boxed{y = Ce^{kx},\quad C \in \mathbb{R}}$$

**Пример 3. Логистическое уравнение** (ограниченный рост популяции):
$$\dot{x} = kx(N-x)$$
Два стационарных решения: $x=0$ (неустойчивое) и $x=N$ (устойчивое). Точка перегиба при $x = N/2$. При $x(0) > 0$ решения стремятся к $N$.

---

## Билет 3. Замена переменных в ДУ

### Общая теория замены

**ДУ:** $y' = f(x,y)$, $G \subset \mathbb{R}^2$.

**Замена:** $(x,y) \to (u,v)$:
$$\begin{cases} x = \alpha(u,v) \\ y = \beta(u,v) \end{cases} \iff \begin{cases} u = \alpha(x,y) \\ v = \beta(x,y) \end{cases}$$

**Требования:** биективность + гладкость (существование частных производных $\alpha'_u, \alpha'_v, \beta'_u, \beta'_v$).

По Лейбницу:
$$\frac{dy}{dx} = f(x,y) \iff \frac{\beta'_u\,du + \beta'_v\,dv}{\alpha'_u\,du + \alpha'_v\,dv} = g(u,v)$$

Раскрывая:
$$\frac{dv}{du} = \frac{g\alpha'_u - \beta'_u}{\beta'_v - g\alpha'_v}, \quad \frac{du}{dv} = \frac{\beta'_v - g\alpha'_v}{g\alpha'_u - \beta'_u}$$

Знаменатели не могут быть равны нулю одновременно, т.к. иначе якобиан замены $= 0$, а замена обратима $\Rightarrow$ якобиан $\neq 0$.

---

### Основные типы замен

**1. «Переворот» ДУ:** $(x,y) \to (y,x)$
$$y' = f(x,y) \to \frac{dx}{dy} = \frac{1}{f(x,y)}$$
Используется, когда ДУ линейно по $x$: $y' = a(y)x + b(y) \xrightarrow{\text{переворот}} \dfrac{dx}{dy} = a(y)x + b(y)$ — линейное по $x$.

**2. Замена $z = ax + by$** для уравнения $y' = f(ax+by)$:
$$z' = a + by' = a + bf(z) \quad \to \text{УРП}$$

**3. Однородное уравнение** $y' = g\!\left(\dfrac{y}{x}\right)$: замена $z = \dfrac{y}{x}$, т.е. $y = xz$:
$$y' = z + xz' = g(z) \Rightarrow xz' = g(z) - z \quad \to \text{УРП}$$

---

## Билет 4. Линейное уравнение. Метод вариации произвольной постоянной

### Постановка

**Линейное неоднородное уравнение (ЛНУ):**
$$y' = a(x)\,y + b(x) \tag{1}$$

**Линейное однородное уравнение (ЛОУ):** при $b(x) = 0$:
$$y' = a(x)\,y \tag{2}$$

Пусть $a(x), b(x) \in C(\langle a,\beta \rangle)$. Естественная область определения: $G = \langle a,\beta \rangle \times \mathbb{R}$.

ЛУ — **не** симметричный тип ($x$ и $y$ не равноправны)! Если нужно, можно «перевернуть»: $\dfrac{dx}{dy} = a(y)x + b(y)$ — ЛУ по $x$.

---

### Метод вариации произвольной постоянной

**Шаг 1.** Решаем ЛОУ (2) — это УРП:
$$\frac{dy}{y} = a(x)\,dx \Rightarrow y = Ce^{\int_{x_0}^x a(t)\,dt}, \quad C \in \mathbb{R}$$

**Шаг 2.** Возврат к ЛНУ. «Варьируем» константу $C$, делая замену $(x,y) \to (x,z)$:
$$y = z \cdot e^{\int_{x_0}^x a(t)\,dt} \tag{3}$$

Это диффеоморфизм: $(3) \Leftrightarrow z = y \cdot e^{-\int_{x_0}^x a(t)\,dt}$, $z \in C^1 \Leftrightarrow y \in C^1$.

**Шаг 3.** Подставляем (3) в (1):
$$z'e^{\int a\,dt} + ze^{\int a\,dt}\cdot a(x) = a(x)\,ze^{\int a\,dt} + b(x)$$
$$\Rightarrow z' = b(x)\,e^{-\int_{x_0}^x a(t)\,dt} \tag{4}$$

Это ДУ первообразной! Из него:
$$z = \int_{x_0}^x b(s)\,e^{-\int_{x_0}^s a(t)\,dt}\,ds + C$$

Подставляем в (3):
$$\boxed{y = e^{\int_{x_0}^x a(t)\,dt}\!\left(C + \int_{x_0}^x b(s)\,e^{-\int_{x_0}^s a(t)\,dt}\,ds\right)} \tag{5}$$

---

> [!theorem] Теорема о ЛНУ
> Пусть $a, b \in C(\langle a,\beta \rangle)$. Тогда:
> 1. Формула (5) задаёт все решения ЛНУ (1) и только их
> 2. $G = \langle a,\beta \rangle \times \mathbb{R}$ — область $\exists!$: через каждую точку $(x_0,y_0)$ проходит единственная инт. кривая:
> $$y = e^{\int_{x_0}^x a(t)\,dt}\!\left(y_0 + \int_{x_0}^x b(s)\,e^{-\int_{x_0}^s a(t)\,dt}\,ds\right) \tag{6}$$

**Доказательство.** П.1: из равносильности замены (3), сводящей (1) к (4), решения которого известны.

П.2: подставляем нач. усл. $y(x_0) = y_0$ в (5):
$$y_0 = e^0\bigl(C + 0\bigr) = C \Rightarrow C = y_0 \text{ — единственно}$$

Подставляем $C = y_0$ в (5), получаем (6). $\blacksquare$

---

## Билет 5. Уравнение в симметричной форме. Особые точки. Расширение понятия решения. Теорема о представлении решения в неявном виде

### Симметричная форма

$$M(x,y)\,dx + N(x,y)\,dy = 0 \tag{1}$$

Пусть $M, N \in C(G)$, $G \subset \mathbb{R}^2$. **Смысл:** в точке $(x_0, y_0)$:
- если $N(x_0,y_0) \neq 0$: $(1) \approx \dfrac{dy}{dx} = -\dfrac{M}{N}$
- если $M(x_0,y_0) \neq 0$: $(1) \approx \dfrac{dx}{dy} = -\dfrac{N}{M}$

Т.е. (1) — символическое объединение двух ДУ.

> [!def] Особая точка
> $(x_0, y_0) \in G$ — **особая точка** ДУ (1), если $M(x_0,y_0) = N(x_0,y_0) = 0$.

---

### Расширенное понятие решения: регулярная кривая

Решение (1) — это не только $y = \varphi(x)$ или $x = \Psi(y)$, но и **параметрически заданная кривая**.

> [!def] Регулярная кривая
> Кривая $\Gamma = (\gamma_1(t), \gamma_2(t))$ называется **регулярной**, если:
> $$\gamma_1, \gamma_2 \in C^1 \quad \text{и} \quad |\dot\gamma_1(t)| + |\dot\gamma_2(t)| \neq 0$$

По ТНФ: если $\dot\gamma_1(t_0) \neq 0$, то в окрестности точки $A = (\gamma_1(t_0), \gamma_2(t_0))$ кривая — график $y = \varphi(x)$. Если $\dot\gamma_2(t_0) \neq 0$ — график $x = \Psi(y)$.

> [!def] Интегральная кривая в симм. форме
> Кривая $\Gamma = (\gamma_1(t), \gamma_2(t))$ — **инт. кривая** ДУ (1), если она регулярна и:
> $$M(\gamma_1(t), \gamma_2(t))\,\dot\gamma_1(t) + N(\gamma_1(t), \gamma_2(t))\,\dot\gamma_2(t) = 0 \quad \forall\,t$$

---

### Теорема о представлении решения в неявном виде

Геометрически: касательный вектор $(\dot\gamma_1, \dot\gamma_2)$ перпендикулярен вектору $(M, N)$ в каждой точке кривой. Если $\exists$ функция $U(x,y)$ такая, что $\nabla U \parallel (M,N)$, то $U$ постоянна вдоль решений.

Если $U$ — интеграл ДУ (определение в билете 6), то $\dfrac{d}{dt}U(\gamma_1(t), \gamma_2(t)) = 0 \Rightarrow U(\gamma_1(t), \gamma_2(t)) \equiv C$.

Т.е. **интегральные кривые лежат на линиях уровня** $U = C$. По ТНФ уравнение $U(x,y) = C$ задаёт регулярную кривую там, где $|U'_x| + |U'_y| \neq 0$.

---

## Билет 6. Интеграл ДУ. Теорема об общем решении

### Определение интеграла ДУ

Рассматриваем ДУ в симм. форме $M\,dx + N\,dy = 0$ в области $G$ без особых точек ($|M|+|N| \neq 0$).

> [!def] Интеграл ДУ
> Функция $U \in C^1(G)$ называется **интегралом ДУ (1)**, если:
> 1. $|U'_x| + |U'_y| \neq 0$ в $G$ (условие регулярности)
> 2. $U'_x N - U'_y M \equiv 0$ в $G$

Смысл условия 2: вектор $\nabla U = (U'_x, U'_y)$ перпендикулярен вектору $(M, N)$ в каждой точке $\Rightarrow$ $\nabla U$ перпендикулярен касательному вектору решений.

---

> [!theorem] Теорема 2 (об интеграле = общем решении)
> Если $U$ — интеграл ДУ (1), то формула $\boxed{U(x,y) = C}$ задаёт множество всех интегральных кривых ДУ (1) и только их (это **общее решение**). При этом $G$ — область $\exists!$.

**Доказательство.** Покажем, что $\forall\,c$ формула $U(x,y) = c$ задаёт реш. = инт. кривую.

Зафиксируем $c$, рассмотрим $\tilde U(x,y) = U(x,y) - c$. Тогда $\{U = c\} = \{\tilde U = 0\}$.

$|\tilde U'_x| + |\tilde U'_y| = |U'_x| + |U'_y| \neq 0$ — выполнено.

$\tilde U'_x N - \tilde U'_y M = U'_x N - U'_y M = 0$ — выполнено.

Значит кривая $\Gamma = \{U = c\}$ — интегральная кривая. Обратно: пусть $\Gamma = (\gamma_1(t), \gamma_2(t))$ — произвольное решение (1). Тогда:
$$M\dot\gamma_1 + N\dot\gamma_2 = 0 \quad \text{и} \quad U'_xN - U'_yM = 0$$

Из этих двух уравнений:
$$\frac{d}{dt}U(\gamma_1, \gamma_2) = U'_x\dot\gamma_1 + U'_y\dot\gamma_2 = 0 \Rightarrow U(\gamma_1(t), \gamma_2(t)) \equiv C$$

Т.е. $\Gamma$ лежит на уровне $U = C$. $\exists!$ — из ТНФ: т.к. $|U'_x| + |U'_y| \neq 0$, то либо $U'_x \neq 0$, либо $U'_y \neq 0 \Rightarrow$ по ТНФ уровень $U = C$ локально — единственная кривая. $\blacksquare$

---

## Билет 7. Уравнение в полных дифференциалах. Теорема 1 об общем решении

### Определение УПД

$$M(x,y)\,dx + N(x,y)\,dy = 0, \quad M,N \in C(G),\quad |M|+|N| \neq 0 \tag{1}$$

> [!def] УПД
> ДУ (1) называется **уравнением в полных дифференциалах (УПД)**, если $\exists\,U \in C^1(G)$:
> $$M\,dx + N\,dy = dU(x,y)$$

Из матанализа $dU = U'_x\,dx + U'_y\,dy$, поэтому:
$$M\,dx + N\,dy = dU \iff \begin{cases} M = U'_x \\ N = U'_y \end{cases} \tag{3}$$

---

> [!theorem] Теорема 1 (об общем решении УПД)
> Пусть (1) — УПД в $G$ без особых точек. Тогда:
> 1. $U(x,y)$ — интеграл (1)
> 2. $U(x,y) = C$ задаёт все решения (1) и только их
> 3. $G$ — область $\exists!$

**Доказательство (нахождение $U$).** Берём $(x_0, y_0) \in G$, $G$ — прямоугольник.

Из первого уравнения системы (3): $U'_x = M$, интегрируем по $x$:
$$U(x,y) = \int_{x_0}^x M(s,y)\,ds + C(y)$$

Подбираем $C(y)$ из второго уравнения (3): $U'_y = N$:
$$\int_{x_0}^x M'_y(s,y)\,ds + C'(y) = N(x,y)$$

Поскольку (1) — УПД, то $M'_y = N'_x$ (доказывается в Т2). По формуле Ньютона–Лейбница:
$$N(x,y) - N(x_0,y) + C'(y) = N(x,y) \Rightarrow C'(y) = N(x_0,y)$$

Берём $C(y) = \int_{y_0}^y N(x_0,s)\,ds$. Итого:

$$\boxed{U(x,y) = \int_{x_0}^x M(s,y)\,ds + \int_{y_0}^y N(x_0,s)\,ds} \tag{5}$$

**Ответ:** $U(x,y) = C$, $C \in \mathbb{R}$. $\blacksquare$

---

### Криволинейный интеграл 2-го рода

$$\int_\Gamma M\,dx + N\,dy \overset{\text{def}}{=} \int_{t_0}^{t_1}\!\left[M(\gamma_1(t),\gamma_2(t))\,\dot\gamma_1(t) + N(\gamma_1(t),\gamma_2(t))\,\dot\gamma_2(t)\right]dt$$

---

### Примеры

**Пример 1:** $x\,dy + y\,dx = 0$.

$(0,0)$ — особая точка, $G = \mathbb{R}^2 \setminus \{(0,0)\}$.

$M = y$, $N = x$. $d(xy) = x\,dy + y\,dx \Rightarrow U(x,y) = xy$.

**Ответ:** $xy = C$ (семейство гипербол; при $C=0$ — оси координат).

**Пример 2:** $\dfrac{x\,dy - y\,dx}{x^2+y^2} = 0$, $G = \mathbb{R}^2 \setminus \{(0,0)\}$.

$M = \dfrac{-y}{x^2+y^2}$, $N = \dfrac{x}{x^2+y^2}$.

$M'_y = N'_x = \dfrac{y^2-x^2}{(x^2+y^2)^2}$ — **УПД**.

Ищем $U$: $U'_x = M \Rightarrow U = -\arctan\dfrac{x}{y} + C(y)$.

Из $U'_y = N$: $C'(y) = 0 \Rightarrow U(x,y) = -\arctan\dfrac{x}{y}$.

**Ответ:** $\arctan\dfrac{x}{y} = C$, $C \in \mathbb{R}$, и $y = 0$.

---

## Билет 8. Критерий уравнения в полных дифференциалах (Теорема 2)

### Формулировка

> [!theorem] Теорема 2 (критерий УПД)
> Пусть $M,N \in C(G)$, $\exists\,M'_y,N'_x \in C(G)$. Тогда:
> 1. Если (1) — УПД $\Rightarrow$ $\boxed{M'_y \equiv N'_x}$ в $G$ **(необходимое условие)**
> 2. Если $G = \langle a,b \rangle \times \langle \alpha,\beta \rangle$ (прямоугольник), то $M'_y \equiv N'_x$ — и **достаточное** условие

*(В общем случае достаточно, чтобы $G$ была односвязной)*

---

### Доказательство

**П.1 (необходимость).** Если (1) — УПД, то $\exists\,U$: $U'_x = M$, $U'_y = N$.

По условию $M'_y = U''_{xy} \in C(G)$ и $N'_x = U''_{yx} \in C(G)$.

По теореме о равенстве смешанных производных: $U''_{xy} = U''_{yx} \Rightarrow M'_y = N'_x$. $\blacksquare$

**П.2 (достаточность).** Предъявим явно $U(x,y)$ формулой (5) из Т1.

Берём $(x_0, y_0) \in G$. Полагаем:
$$U(x,y) = \int_{x_0}^x M(s,y)\,ds + \int_{y_0}^y N(x_0,s)\,ds$$

Проверим, что $U'_x = M$ и $U'_y = N$.

$U'_x = M(x,y)$ — очевидно из формулы.

$U'_y$: дифференцируем первое слагаемое под знаком $\int$:
$$\left(\int_{x_0}^x M(s,y)\,ds\right)'_y = \int_{x_0}^x M'_y(s,y)\,ds \underset{M'_y = N'_x}{=} \int_{x_0}^x N'_x(s,y)\,ds = N(x,y) - N(x_0,y)$$

Второе слагаемое: $\left(\int_{y_0}^y N(x_0,s)\,ds\right)'_y = N(x_0,y)$.

Итого: $U'_y = N(x,y) - N(x_0,y) + N(x_0,y) = N(x,y)$. $\blacksquare$

---

### Замечание об односвязности

В Т2 условие «$G$ — прямоугольник» взято для упрощения доказательства. В общем случае **достаточно** $G$ быть **односвязной** (нет «дырок»).

Пример важности односвязности: $\dfrac{x\,dy - y\,dx}{x^2+y^2} = 0$ на $G = \mathbb{R}^2 \setminus \{0\}$ — область **не** односвязна ($\exists$ петля вокруг нуля), и хотя $M'_y = N'_x$, функция $U = \arctan\dfrac{x}{y}$ **не** является однозначной на всей $G$. Тем не менее на любом односвязном подмножестве $G$ она определена и является первообразной.

---

## Билет 9. Системы ДУ. Основные определения. Два основных типа. Сведение к нормальной системе. Векторная запись. Задача Коши. Единственность

### СДУ, разрешённые относительно старших производных

**СДУ общего вида:**
$$\begin{cases} y_1^{(n_1)} = f_1\!\left(x,\, y_1, y_1', \ldots, y_1^{(n_1-1)},\, y_2, \ldots, y_m^{(n_m-1)}\right) \\ \vdots \\ y_m^{(n_m)} = f_m(\,-\,\|\,-\,) \end{cases} \tag{1}$$

где $x$ — независимая переменная, $y_1(x),\ldots,y_m(x)$ — неизвестные функции, $n_1,\ldots,n_m$ — порядки старших производных. **Порядок** СДУ: $\boxed{n = n_1 + \ldots + n_m}$.

### Два основных типа

**Тип 1: Одно ДУ $n$-го порядка** ($m=1$, $n=n_1$):
$$y^{(n)} = f\!\left(x, y, y', \ldots, y^{(n-1)}\right) \tag{2}$$

**Тип 2: Нормальная СДУ (НСДУ) $n$-го порядка** (все $n_k=1$, $m=n$):
$$\begin{cases} y_1' = f_1(x, y_1, \ldots, y_n) \\ \vdots \\ y_n' = f_n(x, y_1, \ldots, y_n) \end{cases} \tag{3}$$

### Сведение к нормальной системе

> [!important] Предложение
> Любая СДУ вида (1) может быть записана в виде эквивалентной ей НСДУ вида (3).

**Доказательство** (для одного ДУ $n$-го порядка). Вводим новые переменные:
$$z_1 = y,\quad z_2 = y',\quad \ldots,\quad z_n = y^{(n-1)}$$

Тогда: $z_1' = z_2$, $z_2' = z_3$, ..., $z_{n-1}' = z_n$, и $z_n' = y^{(n)} = f(x, z_1, z_2, \ldots, z_n)$.

Получаем НСДУ:
$$(2) \longleftrightarrow \begin{cases} z_1' = z_2 \\ z_2' = z_3 \\ \vdots \\ z_{n-1}' = z_n \\ z_n' = f(x, z_1, \ldots, z_n) \end{cases}$$

**Биекция:** решению $y = \varphi(x)$ ДУ (I) соответствует векторное решение НСДУ:
$$\begin{pmatrix} z_1 \\ \vdots \\ z_n \end{pmatrix} = \begin{pmatrix} \varphi(x) \\ \varphi'(x) \\ \vdots \\ \varphi^{(n-1)}(x) \end{pmatrix}$$

И обратно: если $z_1(x), \ldots, z_n(x)$ — решение НСДУ, то $z_2 = z_1'$, $z_3 = z_2' = z_1''$, ..., и $z_1(x)$ — решение (I). Разные решения НСДУ дают разные решения ДУ (проверяется цепочкой равенств производных). $\blacksquare$

### Векторная запись НСДУ

$$\bar{y}(x) = \begin{pmatrix} y_1(x) \\ \vdots \\ y_n(x) \end{pmatrix}, \quad \bar{f}(x,\bar{y}) = \begin{pmatrix} f_1(x,y_1,\ldots,y_n) \\ \vdots \\ f_n(x,y_1,\ldots,y_n) \end{pmatrix}$$

НСДУ в векторной форме: $\boxed{\bar{y}'(x) = \bar{f}(x, \bar{y}(x))}$

Или в обозначениях с $t$: $\dot{x} = f(t,x)$, $(t,x) \in G \subset \mathbb{R}^{n+1}$, $f\colon G \to \mathbb{R}^n$, $f \in C(G)$.

### Основные определения

> [!def] Решение НСДУ
> $x = \varphi(t)\colon \langle a,b \rangle \to \mathbb{R}^n$ — **решение**, если:
> 1. $\varphi \in C^1(\langle a,b \rangle)$
> 2. $\operatorname{Graf}\varphi \subset G$
> 3. $\varphi'(t) = f(t,\varphi(t))$ на $\langle a,b \rangle$

> [!def] Задача Коши для НСДУ
> $$\begin{cases} \dot{x} = f(t,x) \\ x(t_0) = x_0 \end{cases}$$
> $(t_0, x_0)$ — начальная точка, $\{t_0, x_0\}$ — начальные данные.

> [!def] Единственность
> $(t_0,x_0)$ — **точка единственности**, если $\exists$ решение $\varphi$ на $\langle a,b \rangle \ni t_0$ такое, что $\forall$ другого решения $\Psi$ на $\langle a_1,b_1 \rangle \ni t_0$: $\Psi \equiv \varphi$ на $\langle a,b \rangle \cap \langle a_1,b_1 \rangle$.

---

## Билет 10. Поле направлений. Ломаные Эйлера. Сведение ДУ к разностному уравнению. Теорема Пеано (формулировка)

### Поле направлений

Рассматриваем НСДУ $\dot{x} = f(t,x)$. Даже если решение явно не находится, касательная к интегральной кривой в точке $(t_0,x_0)$ записывается сразу:
$$x = x_0 + f(t_0, x_0)(t - t_0)$$

> [!def] Поле направлений
> Отображение, которое каждой точке $(t_0,x_0) \in G$ сопоставляет касательную к интегральной кривой, проходящей через эту точку, — **поле направлений**.

Интегральная кривая в каждой своей точке касается соответствующей прямой поля направлений.

### Ломаная Эйлера

> [!def] Ломаная Эйлера
> **Ломаная Эйлера** — конечнозвенная ломаная, звенья которой лежат на прямых поля направлений.

**Алгоритм построения** $\ell(t)$ на $[t_0, t_0+T]$:

Берём $N$ большим, $h = \dfrac{T}{N}$ — шаг. Узлы: $t_k = t_0 + kh$.

На $[t_k, t_{k+1}]$:
$$\ell(t) \overset{\text{def}}{=} x_k + f(t_k, x_k)(t - t_k), \qquad x_{k+1} = x_k + f(t_k, x_k)\cdot h$$

### Сведение к разностному уравнению

Из формулы шага:
$$\frac{x(t+h) - x(t)}{h} \approx f(t,x) \implies \boxed{x_{k+1} = x_k + f(t_k, x_k)\cdot h}$$

Это **разностное уравнение** — численный метод Эйлера для ДУ.

При $N \to +\infty$ (т.е. $h \to 0$): $\ell(t) \to \varphi(t)$ — точное решение.

> [!theorem] Теорема Пеано (1890)
> Пусть $f \in C(G)$, $(t_0, x_0) \in G$. Тогда задача Коши $\dot{x} = f(t,x)$, $x(t_0)=x_0$ имеет **хотя бы одно** решение, определённое на некотором отрезке $[t_0-d, t_0+d]$.

**Важно:** из непрерывности $f$ — лишь существование, не единственность (пример Пеано: $y'=3y^{2/3}$ имеет бесконечно много решений с $y(0)=0$).

---

## Билет 11. Интегральное уравнение, эквивалентное задаче Коши

### Постановка

НСДУ $\dot{x} = f(t,x)$, $f \in C(G)$. Задача Коши:
$$\begin{cases} \dot{x} = f(t,x) \\ x(t_0) = x_0 \end{cases} \tag{2}$$

Наряду с (2) рассмотрим **интегральное уравнение (ИУ)**:
$$\boxed{x(t) = x_0 + \int_{t_0}^{t} f(s, x(s))\,ds} \tag{3}$$

> [!def] Решение ИУ
> $x = \varphi(t)$ на $\langle a,b \rangle$ — **решение ИУ (3)**, если:
> 1. $\varphi \in C(\langle a,b \rangle)$
> 2. $\operatorname{Graf}\varphi \subset G$
> 3. $\varphi(t) \equiv x_0 + \int_{t_0}^t f(s,\varphi(s))\,ds$ на $\langle a,b \rangle$

> [!theorem] Теорема об эквивалентности ДУ и ИУ
> $\varphi(t)$ — решение ЗК (2) $\Leftrightarrow$ $\varphi$ — решение ИУ (3).

**Доказательство.**

$(\Rightarrow)$: $\varphi$ — реш. ЗК $\Rightarrow$ $\dot\varphi = f(t,\varphi(t))$. Интегрируем:
$$\varphi(t) - \varphi(t_0) = \int_{t_0}^t f(s,\varphi(s))\,ds \implies \varphi(t) = x_0 + \int_{t_0}^t f(s,\varphi(s))\,ds$$

$(\Leftarrow)$: $\varphi$ — реш. ИУ $\Rightarrow$ $\varphi \in C$. Т.к. $f(s,\varphi(s)) \in C$, то интеграл $\in C^1$ (**эффект повышения гладкости**). Дифференцируем (3): $\dot\varphi(t) = f(t,\varphi(t))$. Начальное условие: $\varphi(t_0) = x_0 + 0 = x_0$. $\blacksquare$

### Пример

$$y(x) = \int_0^x y(s)\,ds + x + 1 \iff \begin{cases} y' = y+1 \\ y(0) = 1 \end{cases}$$

Решение: $y = 2e^x - 1$.

---

## Билет 12. Условие Липшица. Связь локального и глобального условий на компакте

### Определения

$f(t,x)\colon G \to \mathbb{R}^n$, $G$ — область в $\mathbb{R}^{n+1}$.

> [!def] Глобальное условие Липшица
> $f \in \mathrm{Lip}_x(G)$, если $\exists\,L$: $\forall\,(t,\bar{x}),(t,\tilde{x}) \in G$:
> $$\|f(t,\bar{x}) - f(t,\tilde{x})\| \leq L\|\bar{x}-\tilde{x}\|$$

> [!def] Локальное условие Липшица
> $f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$, если $\forall\,(t_0,x_0) \in G$ $\exists$ окр. $U \ni (t_0,x_0)$: $f \in \mathrm{Lip}_x(U)$.

**Примеры:**
- $f = Ax + b$ ($A$ — константная матрица): $f \in \mathrm{Lip}_x(\mathbb{R}^n)$, $L = \|A\|$
- $f = |x|$, $f = \sin x$: $\mathrm{Lip}_x(\mathbb{R})$, $L=1$
- $f = x^2$: **не** $\mathrm{Lip}_x(\mathbb{R})$, но $f \in \mathrm{Lip}_x^{\mathrm{loc}}(\mathbb{R})$

> [!theorem] Теорема 2 (связь локального и глобального на компакте)
> Если $f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$, то $\forall$ компакта $K \subset G$: $f \in \mathrm{Lip}_x(K)$.

**Доказательство (от противного).** Предположим $f \notin \mathrm{Lip}_x(K)$. Тогда:
$$\forall\,n \in \mathbb{N}\;\exists\,(t_n,\bar{x}_n),(t_n,\tilde{x}_n) \in K: \|f(t_n,\bar{x}_n)-f(t_n,\tilde{x}_n)\| > n\|\bar{x}_n-\tilde{x}_n\| \tag{$*$}$$

Из $(*)$: $\|\bar{x}_n-\tilde{x}_n\| \neq 0$. $K$ — компакт $\Rightarrow$ $\exists$ п/п $(t_{n_k},\bar{x}_{n_k}) \to (t^*,\bar{x}^*)$ и $(t_{n_k},\tilde{x}_{n_k}) \to (t^*,\tilde{x}^*)$.

**Случай 1:** $\bar{x}^* \neq \tilde{x}^*$. Из $(*)$ при $n\to\infty$ и $f\in C$: $\|f(t^*,\bar{x}^*)-f(t^*,\tilde{x}^*)\| = +\infty$ — противоречие.

**Случай 2:** $\bar{x}^* = \tilde{x}^*$. По лок. условию Липшица $\exists$ окр. $U \ni (t^*,\bar{x}^*)$ и $L$: $f\in\mathrm{Lip}_x(U)$. При больших $n$ оба $(t_n,\bar{x}_n),(t_n,\tilde{x}_n) \in U \Rightarrow \|f(\ldots)-f(\ldots)\| \leq L\|\bar{x}_n-\tilde{x}_n\|$. Но из $(*)$ при $n > L$ это невозможно. Противоречие. $\blacksquare$

---

## Билет 13. Условие Липшица. Связь с дифференцируемостью

> [!theorem] Теорема 1 (дифференцируемость $\Rightarrow$ Липшиц)
> Если $\exists\, f'_x(t,x) \in C(G) \Rightarrow f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$.

**Доказательство.** Берём $(t_0,x_0) \in G$. $\exists\,B_r(t_0,x_0) \subset G$: $f'_x \in C(\bar{B}_r)$ — компакт $\Rightarrow$ по Т. Вейерштрасса $\exists\,\max_{\bar{B}_r}\|f'_x\| = L$.

Покажем $f \in \mathrm{Lip}_x(B_r)$ с константой $L$. Берём $(t,\bar{x}),(t,\tilde{x}) \in B_r$.

Введём $g(\theta) = f(t, \theta\bar{x}+(1-\theta)\tilde{x})$, $\theta \in [0,1]$.

По **формуле конечных приращений**:
$$f(t,\bar{x}) - f(t,\tilde{x}) = g(1)-g(0) = \int_0^1 g'(\theta)\,d\theta = \int_0^1 f'_x(t,\theta\bar{x}+(1-\theta)\tilde{x})\,(\bar{x}-\tilde{x})\,d\theta$$

Берём норму:
$$\|f(t,\bar{x})-f(t,\tilde{x})\| \leq \int_0^1 \|f'_x(\ldots)\|\,d\theta\cdot\|\bar{x}-\tilde{x}\| \leq L\|\bar{x}-\tilde{x}\| \quad \blacksquare$$

**Замечание о соотношении классов:**
$$\mathrm{Lip}_x(G) \subset \mathrm{Lip}_x^{\mathrm{loc}}(G) \subset C_x, \qquad C_x^1 \subset \mathrm{Lip}_x^{\mathrm{loc}}(G)$$

$$\boxed{C_x \not\Rightarrow \mathrm{Lip}_x^{\mathrm{loc}}, \quad \mathrm{Lip}_x^{\mathrm{loc}} \not\Rightarrow C_x^1}$$

**Примеры:**
- $f = x^2$: $f'_x = 2x \in C(\mathbb{R}) \Rightarrow f \in \mathrm{Lip}_x^{\mathrm{loc}}(\mathbb{R})$, но $f \notin \mathrm{Lip}_x(\mathbb{R})$
- Линейная система $f = A(t)x + B(t)$: $f'_x = A(t) \in C \Rightarrow \mathrm{Lip}_x^{\mathrm{loc}}$; глобально — если $\|A(t)\|$ ограничено

---

## Билет 14. Теорема Пикара: пикаровские приближения, сходимость, существование, неравенство Гронуолла, единственность

### Формулировка

> [!theorem] Теорема Пикара
> Пусть $f \in C(G)$, $f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$. Тогда $G$ — область $\exists!$: $\forall\,(t_0,x_0) \in G$ ЗК имеет единственное решение на отрезке Пеано $I = [t_0-h, t_0+h]$.

### Построение отрезка Пеано

Цилиндр $P = \{(t,x)\mid |t-t_0|\leq a, \|x-x_0\|\leq b\} \subset G$.

$M = \max_P\|f\|$, $h = \min\!\left\{a, \dfrac{b}{M}\right\}$, $I = [t_0-h, t_0+h]$.

### Пикаровские приближения

$$\varphi_0(t) = x_0, \qquad \varphi_{k+1}(t) = x_0 + \int_{t_0}^t f(s,\varphi_k(s))\,ds$$

**Лемма 1** (все $\varphi_k$ определены на $I$, $\operatorname{Graf}\varphi_k \subset P$): по индукции; оценка $\|\varphi_{k+1}(t)-x_0\| \leq M|t-t_0| \leq Mh \leq b$.

**Лемма 2** (оценка шага):
$$\|\varphi_{k+1}(t) - \varphi_k(t)\| \leq \frac{M}{L}\cdot\frac{L^{k+1}|t-t_0|^{k+1}}{(k+1)!}$$

Доказательство — индукция; на каждом шаге применяем условие Липшица (5) и предыдущую оценку.

**Лемма 3** (сходимость): представляем $\varphi_k$ как частичную сумму ряда:
$$\varphi_k = \varphi_0 + \sum_{j=0}^{k-1}(\varphi_{j+1}-\varphi_j)$$

По критерию Вейерштрасса ряд равномерно сходится на $I$, т.к. мажорируется числовым рядом:
$$\|x_0\| + \frac{M}{L}(e^{Lh}-1) < \infty$$

Предел: $\varphi(t) \overset{\text{def}}{=} \lim_{k\to\infty}\varphi_k(t)$.

**Лемма 4** ($\varphi$ — решение ИУ): т.к. $\varphi_k \rightrightarrows \varphi$, можно перейти к пределу под знаком интеграла (с оценкой через Липшиц), получаем $\varphi(t) = x_0 + \int_{t_0}^t f(s,\varphi(s))\,ds$.

### Неравенство Гронуолла (Лемма 5)

> [!theorem] Неравенство Гронуолла
> Пусть $u(t) \geq 0$, $u \in C$, и $\forall\,t$:
> $$u(t) \leq \alpha + \beta\left|\int_{t_0}^t u(s)\,ds\right|, \quad \alpha,\beta \geq 0$$
> Тогда $u(t) \leq \alpha e^{\beta|t-t_0|}$.
>
> **Следствие:** если $\alpha = 0$, то $u(t) \equiv 0$.

**Доказательство** (для $t \geq t_0$): $V(t) = \alpha + \beta\int_{t_0}^t u(s)\,ds$, тогда $u \leq V$ и $\dot V = \beta u \leq \beta V$.

$\Rightarrow (Ve^{-\beta t})' \leq 0 \Rightarrow V(t)e^{-\beta t} \leq V(t_0)e^{-\beta t_0} = \alpha e^{-\beta t_0}$.

$\Rightarrow V(t) \leq \alpha e^{\beta(t-t_0)} \Rightarrow u(t) \leq \alpha e^{\beta(t-t_0)}$. $\blacksquare$

### Единственность (Лемма 6)

Пусть $\varphi_1, \varphi_2$ — два решения ЗК на $\langle a,b \rangle$. Пусть $\exists\,t^* > t_0$: $\varphi_1(t^*) \neq \varphi_2(t^*)$.

$u(t) = \|\varphi_1(t)-\varphi_2(t)\|$ на $[t_0,t^*]$. По ИУ:
$$u(t) \leq \int_{t_0}^t L\|\varphi_1(s)-\varphi_2(s)\|\,ds = L\int_{t_0}^t u(s)\,ds$$

По Гронуоллу с $\alpha=0$: $u(t) \equiv 0$ на $[t_0,t^*] \Rightarrow \varphi_1(t^*) = \varphi_2(t^*)$ — противоречие. $\blacksquare$

**Т.о. теорема Пикара доказана.**

---

## Билет 15. Продолжимость решений. Определение. Необходимое и достаточное условие продолжимости за точку (Т1)

### Определения

> [!def] Продолжимость
> Решение $\varphi$ на $\langle a,b \rangle$ **продолжимо вправо за т. $b$**, если $\exists$ решение $\Psi$ на $\langle a,b_1 \rangle$ ($b_1 > b$): $\varphi \equiv \Psi$ на $\langle a,b \rangle$.
>
> $\Psi$ — **продолжение** $\varphi$; $\varphi$ — **сужение** $\Psi$.

> [!def] Максимально продолженное (полное) решение
> Решение $\varphi$ на $\langle a,b \rangle$ **полное**, если оно не является сужением никакого другого решения.

**Пример:** $\dot x = x$, $x(0)=1$.
- $\varphi_1(t) = e^t$, $t \in \mathbb{R}$ — полное решение
- $\varphi_2(t) = e^t$, $t \in (-1,+\infty)$ — не полное (сужение $\varphi_1$)

### Теорема (необходимое и достаточное условие)

> [!theorem] Теорема 1
> Пусть $\varphi$ — решение на $\langle a,b \rangle$. Тогда $\varphi$ **продолжимо вправо за т. $b$** $\Leftrightarrow$
> $$\exists\,\lim_{t\to b-0}(t,\varphi(t)) = (b, x^*) \in G$$

**Доказательство.**

$(\Rightarrow)$: $\exists$ продолжение $\Psi$ на $\langle a,b_1 \rangle$: $\varphi \equiv \Psi$ на $\langle a,b \rangle$.
$$\lim_{t\to b-0}(t,\varphi(t)) = \lim_{t\to b-0}(t,\Psi(t)) = (b,\Psi(b)) \in G$$
т.к. $b \in \langle a,b_1 \rangle$ — внутренняя точка, граф $\Psi \subset G$.

$(\Leftarrow)$: $\lim_{t\to b-0}(t,\varphi(t)) = (b,x^*) \in G$. Решаем ЗК:
$$\begin{cases}\dot x = f(t,x) \\ x(b) = x^*\end{cases}$$

По теореме Пеано: $\exists$ решение $\Psi_1$ на $[b-h, b+h]$.

Строим **склейку**:
$$\Psi(t) = \begin{cases} \varphi(t), & t \in \langle a,b \rangle \\ \Psi_1(t), & t \in [b, b+h] \end{cases}$$

$\Psi$ непрерывна в $b$ (т.к. $\lim_{t\to b}\varphi(t) = x^* = \Psi_1(b)$), удовлетворяет ДУ на $\langle a,b+h \rangle$ (по эквивалентности с ИУ). Т.о. $\Psi$ — продолжение $\varphi$ за $b$. $\blacksquare$

### Следствие

Полное решение на $\langle a,b \rangle$: либо $b = +\infty$, либо $(t,\varphi(t))$ «уходит» на границу $G$ при $t \to b-0$ (т.е. предел не попадает в $G$).

---

## Билет 16. Достаточное условие продолжимости (Т2). Теорема о существовании полного решения в условиях теоремы Пикара (Т3)

### Т2: Достаточное условие продолжимости

*(Используется неравенство Гронуолла)*

> [!theorem] Теорема 2
> Пусть $f \in C(G)$, $f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$, $\varphi$ — решение ЗК на $[t_0, b)$. Если $\exists$ константа $\lambda > 0$ такая, что $\|\varphi(t)\| \leq \lambda e^{\lambda|t-t_0|}$ (т.е. решение не «улетает» на $\infty$ слишком быстро), то $\varphi$ продолжимо вправо за $b$.

*(Полная формулировка и доказательство — через неравенство Гронуолла и Т1)*

### Т3: Существование полного решения

> [!theorem] Теорема 3
> В условиях теоремы Пикара ($f \in C(G)$, $f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$): $\forall\,(t_0,x_0) \in G$ существует **полное** (максимально продолженное) решение ЗК.

**Идея доказательства:** Рассмотрим множество всех решений ЗК. По теореме Пикара каждое два решения совпадают на пересечении своих областей определения. Берём «объединение» всех решений — это и есть полное решение. Его область определения — открытый интервал $(a,b)$; если $b < +\infty$, то $(t,\varphi(t))$ выходит на $\partial G$ при $t \to b$.

### Теорема о продолжимости почти линейных систем

> [!theorem]
> Если $f(t,x) = A(t)x + B(t)$, где $A(t), B(t)$ непрерывны на $\langle a,b \rangle$, то любое решение ЗК продолжимо на весь $\langle a,b \rangle$ (т.е. не «вылетает» за конечное время).

**Смысл:** для линейных систем решение «живёт» столько же, сколько задана область — не укорачивается.

---

## Билет 17. Линейные системы и уравнения: теорема о существовании максимально продолженных решений. Сведение ЛУ к ЛС

### Линейная система (ЛНС)

$$\dot x = A(t)x + f(t) \tag{1}$$

$x \in \mathbb{R}^n$, $A(t)$ — $(n\times n)$-матрица, $A(t) \in C((a,b))$, $f(t) \in C((a,b))$.

Естественная обл. опр.: $G = (a,b)\times\mathbb{R}^n$.

> [!theorem] Теорема о $\exists!$ для ЛНС
> Пусть $A(t) \in C((a,b))$, $f(t) \in C((a,b))$. Тогда $G = (a,b)\times\mathbb{R}^n$ — обл. $\exists!$, т.е. $\forall\,(t_0,x_0)\in G$ ЗК $x(t_0)=x_0$ имеет **единственное решение**, причём **максимально продолженное**, определённое на всём $(a,b)$.

**Доказательство.** Проверяем условия Т. Пикара:

$f(t,x) = A(t)x + f(t)$ — непрерывна в $G$ как сумма непрерывных.

$\exists\,f'_x = A(t) \in C(G) \Rightarrow f \in \mathrm{Lip}_x^{\mathrm{loc}}(G)$ (по Т1 §4). Т.о. условия Т. Пикара выполнены $\Rightarrow$ $\exists!$ реш.

Для продолжимости на весь $(a,b)$: применяем Т4 (о почти линейных системах), т.к.:
$$\|f(t,x)\| \leq \|A(t)\|\|x\| + \|f(t)\| = \alpha(t)\|x\| + \beta(t)$$

Т.о. ЛС — «почти линейная», и $(a,b)$ — максимальный промежуток $\exists$-я $\forall$ решения. $\blacksquare$

### Сведение линейного уравнения $n$-го порядка к ЛС

**ЛНУ:**
$$x^{(n)} + a_{n-1}(t)x^{(n-1)} + \ldots + a_1(t)\dot x + a_0(t)x = f(t) \tag{2}$$

Замена: $y_1 = x$, $y_2 = \dot x$, ..., $y_n = x^{(n-1)}$:

$$(2) \leftrightarrow \dot y = A(t)y + F(t) \tag{3}$$

$$A(t) = \begin{pmatrix} 0 & 1 & 0 & \cdots & 0 \\ 0 & 0 & 1 & \cdots & 0 \\ \vdots & & & \ddots & \vdots \\ 0 & 0 & 0 & \cdots & 1 \\ -a_0 & -a_1 & \cdots & -a_{n-2} & -a_{n-1} \end{pmatrix}, \quad F(t) = \begin{pmatrix} 0 \\ \vdots \\ 0 \\ f(t) \end{pmatrix}$$

$(2) \sim (3)$. Из теоремы для ЛНС: все решения (2) продолжимы на весь $(a,b)$.

---

## Билет 18. ЛОС: линейность пространства решений, ЛНЗ решений, теорема об изоморфизме и следствия

### Линейность пространства решений

**ЛОС:** $\dot x = A(t)x$ **(1)**, $M^\circ$ — мн-во всех решений.

> [!theorem] Теорема 1
> Любая линейная комбинация решений ЛОС есть снова решение. Т.е. $M^\circ$ — **линейное пространство**.

**Доказательство:**
$$\frac{d}{dt}(c_1\varphi_1 + c_2\varphi_2) = c_1\dot\varphi_1 + c_2\dot\varphi_2 = c_1A\varphi_1 + c_2A\varphi_2 = A(c_1\varphi_1+c_2\varphi_2) \quad\blacksquare$$

### ЛНЗ решений

> [!def] ЛНЗ функций
> $\{\varphi_1,\ldots,\varphi_m\}$ — **ЛНЗ** на $(a,b)$, если $\sum c_k\varphi_k(t)\equiv 0 \Rightarrow \forall\,c_k=0$.

**Важно:** для решений ЛОС ЛНЗ как функций $\Leftrightarrow$ ЛНЗ как векторы в $\mathbb{R}^n$ при одном $t_0$.

### Теорема об изоморфизме

> [!theorem] Теорема 2 (об изоморфизме)
> $M^\circ \cong \mathbb{R}^n$, где $n$ — порядок ЛОС.

**Явный изоморфизм:** $h\colon M^\circ \to \mathbb{R}^n$, $h(\varphi) = \varphi(t_0)$ (для фикс. $t_0$).

**Доказательство:**
1. **Линейность:** $h(c_1\varphi_1+c_2\varphi_2) = (c_1\varphi_1+c_2\varphi_2)(t_0) = c_1h(\varphi_1)+c_2h(\varphi_2)$.
2. **Инъекция:** $h(\varphi_1)=h(\varphi_2) \Rightarrow \varphi_1(t_0)=\varphi_2(t_0) \Rightarrow$ реш. одной ЗК $\Rightarrow \varphi_1\equiv\varphi_2$ (единственность).
3. **Сюръекция:** $\forall\,v\in\mathbb{R}^n$ решаем ЗК $\dot x=A(t)x$, $x(t_0)=v$ $\Rightarrow \exists!\,\varphi$: $h(\varphi)=v$. $\blacksquare$

### Следствия

**Следствие 1 (размерность):** $\dim M^\circ = n$.

**Следствие 2 (критерий ЛНЗ):** реш. $\varphi_1,\ldots,\varphi_m$ — ЛНЗ как функции $\Leftrightarrow$ $\varphi_1(t_0),\ldots,\varphi_m(t_0)$ — ЛНЗ как векторы.

**Следствие 3 (ФСР существует):** любые $n$ ЛНЗ решений ЛОС образуют **базис** $M^\circ$ = ФСР.

**Формула общего решения ЛОС:**
$$x_{\text{оо}} = c_1\varphi_1(t) + \ldots + c_n\varphi_n(t)$$
где $\varphi_1,\ldots,\varphi_n$ — ФСР, $c_k\in\mathbb{R}$ произвольные.

### Вронскиан

$$W_{\varphi_1,\ldots,\varphi_n}(t) = \det(\varphi_1(t),\ldots,\varphi_n(t))$$

> [!theorem] Теорема о Вронскиане
> Для решений ЛОС эквивалентны:
> 1. $\exists\,t_0$: $W(t_0)=0$
> 2. $W(t)\equiv 0$ на $(a,b)$
> 3. $\varphi_1,\ldots,\varphi_n$ — ЛЗ

Следствие: $\varphi_1,\ldots,\varphi_n$ — ФСР $\Leftrightarrow$ $W(t)\neq 0$ $\forall\,t$.

---

## Билет 19. Переформулировки результатов для ЛОУ. Вронскиан

**ЛОУ $n$-го порядка:**
$$x^{(n)} + a_{n-1}(t)x^{(n-1)} + \ldots + a_0(t)x = 0 \tag{1}$$

Пространство решений $M^\circ$ — линейное (оператор $L$ — линейный: $L(c_1\varphi_1+c_2\varphi_2)=c_1L\varphi_1+c_2L\varphi_2$).

### Теорема об изоморфизме для ЛОУ

> [!theorem] Теорема 2 (для ЛОУ)
> $M^\circ \cong \mathbb{R}^n$.

**Явный изоморфизм:** $h(\varphi) = \begin{pmatrix}\varphi(t_0)\\\varphi'(t_0)\\\vdots\\\varphi^{(n-1)}(t_0)\end{pmatrix}$, $h\colon M^\circ\to\mathbb{R}^n$.

Доказательство аналогично случаю ЛОС:
1. Линейность — очевидно.
2. Инъекция: $h(\varphi_1)=h(\varphi_2) \Rightarrow$ совпадают нач. данные ЗК $\Rightarrow \varphi_1\equiv\varphi_2$.
3. Сюръекция: $\forall\,v=(v_0,\ldots,v_{n-1})\in\mathbb{R}^n$ реш. ЗК $x(t_0)=v_0,\ldots,x^{(n-1)}(t_0)=v_{n-1}$ $\Rightarrow \exists!\,\varphi$. $\blacksquare$

### Следствия и Вронскиан

**Критерий ЛНЗ реш. ЛОУ:** реш. $\varphi_1,\ldots,\varphi_m$ — ЛНЗ на $(a,b)$ $\Leftrightarrow$ векторы $\begin{pmatrix}\varphi_i(t_0)\\\vdots\\\varphi_i^{(n-1)}(t_0)\end{pmatrix}$ — ЛНЗ в $\mathbb{R}^n$.

**Вронскиан для ЛОУ:**
$$W_{\varphi_1,\ldots,\varphi_n}(t) = \begin{vmatrix} \varphi_1 & \cdots & \varphi_n \\ \dot\varphi_1 & \cdots & \dot\varphi_n \\ \vdots & & \vdots \\ \varphi_1^{(n-1)} & \cdots & \varphi_n^{(n-1)} \end{vmatrix}$$

$\varphi_1,\ldots,\varphi_n$ — ФСР $\Leftrightarrow$ $W\neq 0$ $\forall\,t\in(a,b)$.

**Формула общего решения ЛОУ:**
$$x_{\text{оо}} = c_1\varphi_1(t) + \ldots + c_n\varphi_n(t), \quad c_k\in\mathbb{R}$$

**Формула Лиувилля–Остроградского для ЛОУ:** из Т2 для ЛОС, применённой к соответствующей системе:
$$W(t) = W(t_0)\,e^{-\int_{t_0}^t a_{n-1}(s)\,ds}$$
(след матрицы $A(t)$ равен $-a_{n-1}(t)$).

---

## Билет 20. ЛОУ с постоянными коэффициентами. Метод Эйлера. Характеристический многочлен. Три случая корней

**ЛОУ с постоянными коэффициентами:**
$$a_n x^{(n)} + a_{n-1}x^{(n-1)} + \ldots + a_1\dot x + a_0 x = 0 \tag{1}, \quad a_k\in\mathbb{R}$$

$G=(-\infty,+\infty)\times\mathbb{R}^n$ — обл. $\exists!$, все реш. определены на $(-\infty,+\infty)$.

### Характеристический многочлен и метод Эйлера

Ищем реш. $x = e^{\lambda t}$. Т.к. $(e^{\lambda t})^{(k)} = \lambda^k e^{\lambda t}$:
$$L(e^{\lambda t}) = (a_n\lambda^n + \ldots + a_1\lambda + a_0)e^{\lambda t} = \chi(\lambda)e^{\lambda t}$$

> [!def] Характеристический многочлен
> $\chi(\lambda) = a_n\lambda^n + \ldots + a_1\lambda + a_0$ — **характеристический многочлен** ЛОУ.

> [!theorem] Лемма 1
> $e^{\lambda t}$ — реш. (1) $\Leftrightarrow$ $\lambda$ — корень $\chi$.

### Случай 1: все корни вещественные и простые

$\lambda_1 < \lambda_2 < \ldots < \lambda_n$ — корни $\chi$.

По Л1: $e^{\lambda_1 t},\ldots,e^{\lambda_n t}$ — $n$ решений. Они ЛНЗ (делением на $e^{\lambda_n t}$ и пределом при $t\to+\infty$).

**Ответ:** $x = \sum_{k=1}^n c_k e^{\lambda_k t}$.

### Случай 2: простые комплексные корни

Если $\chi$ имеет вещ. коэф. и $\lambda=\alpha+i\beta$ — корень, то $\bar\lambda=\alpha-i\beta$ — тоже корень.

$e^{(\alpha+i\beta)t}$ — $\mathbb{C}$-решение. Овеществляем:

> [!theorem] Лемма 2
> Если $\varphi(t)=u(t)+iv(t)$ — $\mathbb{C}$-реш. ЛОУ с вещ. коэф., то $u(t)=\operatorname{Re}\varphi(t)$ и $v(t)=\operatorname{Im}\varphi(t)$ — $\mathbb{R}$-решения.

По формуле Эйлера: $e^{(\alpha+i\beta)t} = e^{\alpha t}(\cos\beta t + i\sin\beta t)$.

Паре корней $\alpha\pm i\beta$ соответствует пара $\mathbb{R}$-решений: $\{e^{\alpha t}\cos\beta t,\; e^{\alpha t}\sin\beta t\}$.

### Случай 3: кратный корень кратности $k$

> [!theorem] Лемма 3
> Если $\lambda_1$ — корень $\chi$ кратности $k$, то $e^{\lambda_1 t}, te^{\lambda_1 t}, \ldots, t^{k-1}e^{\lambda_1 t}$ — решения ЛОУ.

**Доказательство:** $L(t^m e^{\lambda t}) = \dfrac{\partial^m}{\partial\lambda^m}(\chi(\lambda)e^{\lambda t})\big|_{\lambda=\lambda_1}$. Т.к. $\chi(\lambda_1)=\chi'(\lambda_1)=\ldots=\chi^{(k-1)}(\lambda_1)=0$, то при $m\leq k-1$ все слагаемые равны нулю. $\blacksquare$

Если $\lambda_1=\alpha\pm i\beta$ — пара комплексных кратных корней кратности $k$ (суммарно $2k$):
$$\{e^{\alpha t}\cos\beta t,\; e^{\alpha t}\sin\beta t,\; te^{\alpha t}\cos\beta t,\; te^{\alpha t}\sin\beta t,\; \ldots,\; t^{k-1}e^{\alpha t}\cos\beta t,\; t^{k-1}e^{\alpha t}\sin\beta t\}$$

**Пример:** корни $\lambda_1=0$ кр. 3, $\lambda_2=\pm i$ кр. 2, $\lambda_3=-2$ кр. 1.

ФСР: $\{1, t, t^2, \cos t, \sin t, t\cos t, t\sin t, e^{-2t}\}$.

**Ответ:** $x = c_1+c_2 t+c_3 t^2+(c_4+c_5 t)\cos t+(c_6+c_7 t)\sin t+c_8 e^{-2t}$.

---

## Билет 21. Фундаментальная матрица ЛОС. Матричная запись общего решения. Теоремы о ФМ. Формула Лиувилля–Остроградского

### Фундаментальная матрица

**ЛОС:** $\dot x = A(t)x$, ФСР: $\varphi_1(t),\ldots,\varphi_n(t)$.

> [!def] Фундаментальная матрица
> $\Phi(t) = (\varphi_1(t),\ldots,\varphi_n(t))$ — **фундаментальная матрица (ФМ)**.

**Матричная запись общего решения:**
$$\boxed{x_{\text{оо}} = \Phi(t)C}, \quad C = \begin{pmatrix}c_1\\\vdots\\c_n\end{pmatrix}\in\mathbb{R}^n$$

**Матричное ДУ:** $\dot X = A(t)X$. ФМ — невырожденное решение этого МДУ.

> [!theorem] Лемма 1
> $X(t)=(\varphi_1(t),\ldots,\varphi_n(t))$ — реш. МДУ $\Leftrightarrow$ каждый столбец $\varphi_k$ — реш. ЛОС.

> [!theorem] Теорема 1
> $\Phi(t)$ — ФМ ЛОС $\Leftrightarrow$ $\Phi(t)$ — **невырожденное** решение МДУ $\dot X=A(t)X$.

> [!theorem] Лемма 2
> Если $\Phi(t)$ — ФМ и $S$ — невырожденная постоянная матрица, то $\Phi(t)S$ — снова ФМ.

**Доказательство:** $\det(\Phi S)=\det\Phi\cdot\det S\neq 0$; $\frac{d}{dt}(\Phi S)=\dot\Phi S=(A\Phi)S=A(\Phi S)$. $\blacksquare$

### Формула Лиувилля–Остроградского

> [!theorem] Теорема 2 (Лиувилля–Остроградского)
> $$\boxed{W(t) = W(t_0)\,e^{\int_{t_0}^t \operatorname{tr}A(s)\,ds}}$$
> где $W(t)=\det\Phi(t)$ — Вронскиан, $\operatorname{tr}A = \sum_{k=1}^n a_{kk}$ — след матрицы.

**Геометрический смысл:** параллелепипед, образованный столбцами ФМ, меняет объём с экспоненциальной скоростью $\operatorname{tr}A$.

**Следствие:** $W(t)\neq 0$ для $\forall\,t$ (если $W(t_0)\neq 0$) — подтверждает, что ФМ всегда невырождена.

---

## Билет 22. ЛНС: теорема о множестве решений, метод вариации, формулы Лагранжа и Коши

### ЛНС и структура множества решений

$$\dot x = A(t)x + f(t) \tag{1}, \quad A(t),f(t)\in C((a,b))$$

$G = (a,b)\times\mathbb{R}^n$ — обл. $\exists!$.

> [!theorem] Теорема 1 (о структуре $M^H$)
> Пусть $\psi_{\text{ч}}(t)$ — одно частное решение ЛНС (1). Тогда:
> $$M^H = M^\circ + \psi_{\text{ч}}, \qquad x_{\text{оо}} = x_{\text{оо}}^{\text{ЛОС}} + \psi_{\text{ч}}$$

**Доказательство.** Замена $y = x - \psi_{\text{ч}}(t)$:
$$\dot y = \dot x - \dot\psi_{\text{ч}} = (Ax+f) - (A\psi_{\text{ч}}+f) = Ay$$
Замена сводит ЛНС к ЛОС. Обратная замена $x = y+\psi_{\text{ч}}$ даёт все решения ЛНС. $\blacksquare$

**Следствия:** $M^H$ — аффинное пространство; разность двух решений ЛНС — решение ЛОС.

**Формула общего решения ЛНС:** если $\varphi_1,\ldots,\varphi_n$ — ФСР ЛОС:
$$\boxed{x_{\text{оо}} = \sum_{k=1}^n c_k\varphi_k(t) + \psi_{\text{ч}}(t)} \quad \text{или} \quad x_{\text{оо}} = \Phi(t)C + \psi_{\text{ч}}(t)$$

### Метод Лагранжа (вариации произвольных постоянных)

Ищем $\psi_{\text{ч}}(t) = \Phi(t)C(t)$, где $C(t)$ — вектор-функция. Подставляя в ЛНС и используя $\dot\Phi = A\Phi$:
$$\Phi(t)\dot C(t) = f(t) \implies \dot C(t) = \Phi^{-1}(t)f(t)$$

$$\boxed{\psi_{\text{ч}}(t) = \Phi(t)\int_{t_0}^t \Phi^{-1}(s)f(s)\,ds}$$

### Формула Лагранжа и формула Коши

**Формула Лагранжа** (общее решение):
$$x_{\text{оо}} = \Phi(t)C + \Phi(t)\int_{t_0}^t \Phi^{-1}(s)f(s)\,ds$$

**Формула Коши** (решение ЗК $x(t_0)=x_0$, $C = \Phi^{-1}(t_0)x_0$):
$$\boxed{x(t,t_0,x_0) = \Phi(t)\Phi^{-1}(t_0)x_0 + \Phi(t)\int_{t_0}^t\Phi^{-1}(s)f(s)\,ds}$$

Для ЛНС с постоянной матрицей ($\Phi(t)=e^{At}$, $\Phi^{-1}(t)=e^{-At}$):
$$x(t,t_0,x_0) = e^{A(t-t_0)}x_0 + e^{At}\int_{t_0}^t e^{-As}f(s)\,ds$$

---

## Билет 23. Переформулировка результатов для ЛНУ (теорема Лагранжа)

**ЛНУ:**
$$a_n(t)x^{(n)} + \ldots + a_0(t)x = f(t) \tag{1}$$

Пусть $a_k(t),f(t)\in C((a,b))$, $a_n(t)\neq 0$.

### Структура множества решений ЛНУ

Аналогично ЛНС (сводим ЛНУ к ЛНС): если $\psi_{\text{ч}}$ — одно частное решение ЛНУ:
$$M^H = M^\circ + \psi_{\text{ч}}, \qquad x_{\text{оо}} = \sum_{k=1}^n c_k\varphi_k(t) + \psi_{\text{ч}}(t)$$

где $\varphi_1,\ldots,\varphi_n$ — ФСР ЛОУ.

### Теорема Лагранжа для ЛНУ

> [!theorem] Теорема Лагранжа
> Чтобы $\psi_{\text{ч}}(t) = \sum_{k=1}^n c_k(t)\varphi_k(t)$ было решением ЛНУ (1), необходимо и достаточно, чтобы $\dot c_k(t)$ удовлетворяли системе:
> $$\begin{cases} \sum_{k=1}^n \dot c_k\varphi_k = 0 \\ \sum_{k=1}^n \dot c_k\dot\varphi_k = 0 \\ \quad\vdots \\ \sum_{k=1}^n \dot c_k\varphi_k^{(n-2)} = 0 \\ \sum_{k=1}^n \dot c_k\varphi_k^{(n-1)} = \dfrac{f(t)}{a_n(t)} \end{cases}$$

Определитель системы — Вронскиан $W(t)\neq 0$ $\Rightarrow$ система всегда имеет единственное решение $\Rightarrow$ можно найти $\dot c_k$, проинтегрировать, получить $\psi_{\text{ч}}$.

**Замечание:** эта система эквивалентна $\Phi(t)\dot C(t) = \tilde f(t)$ из §6 (в компонентах), где $\tilde f=(0,\ldots,0,f/a_n)^T$.

### Принцип суммы

Если $\psi_1$ — реш. $L(x)=f_1$ и $\psi_2$ — реш. $L(x)=f_2$, то $\psi_1+\psi_2$ — реш. $L(x)=f_1+f_2$: $L(\psi_1+\psi_2)=f_1+f_2$. Позволяет разбивать сложную правую часть на части.

---

## Билет 24. ЛНУ с постоянными коэффициентами. Метод неопределённых коэффициентов (МНК)

**ЛНУ с пост. коэф.:**
$$a_n x^{(n)} + \ldots + a_0 x = f(t), \quad a_k\in\mathbb{R} \tag{5}$$

Правая часть **специального вида:** $f(t) = e^{\alpha t}(P(t)\cos\beta t + Q(t)\sin\beta t)$, $P,Q$ — многочлены.

> [!def] Контрольное число
> $\sigma = \alpha+i\beta$. $s = \begin{cases}0, & \sigma\text{ — не корень }\chi\text{ (нерезонанс)}\\k, & \sigma\text{ — корень }\chi\text{ кратности }k\text{ (резонанс)}\end{cases}$

> [!theorem] Теорема 3 (МНК)
> ЛНУ (5) имеет единственное $\psi_{\text{ч}}$ вида:
> $$\psi_{\text{ч}}(t) = t^s e^{\alpha t}(\tilde P(t)\cos\beta t + \tilde Q(t)\sin\beta t)$$
> где $\tilde P, \tilde Q$ — многочлены, $\deg\tilde P = \deg\tilde Q = \max\{\deg P, \deg Q\}$.

**Алгоритм:** записываем $\psi_{\text{ч}}$ с неизвестными коэффициентами, подставляем в ЛНУ, приравниваем коэффициенты.

**Пример 1 (нерезонанс):** $\ddot x - x = t+1$.

$\chi(\lambda)=\lambda^2-1$, $\sigma=0$ — не корень ($\chi(0)=-1\neq 0$), $s=0$, $\deg P=1$.

$\psi_{\text{ч}} = at+b$. Подст.: $-(at+b)=t+1 \Rightarrow a=-1, b=-1$. **Ответ:** $\psi_{\text{ч}}=-t-1$.

**Пример 2 (резонанс):** $\ddot x - x = e^t$.

$\sigma=1$ — корень $\chi$ кратности 1, $s=1$, $\deg P=0$.

$\psi_{\text{ч}} = Ate^t$. Подст.: $2Ae^t=e^t \Rightarrow A=\tfrac{1}{2}$. **Ответ:** $\psi_{\text{ч}}=\tfrac{1}{2}te^t$.

**Принцип суммы:** $f=f_1+f_2 \Rightarrow \psi_{\text{ч}}=\psi_1+\psi_2$.

---

## Билет 25. ЛОС с постоянной матрицей. Метод Эйлера: простые с.з. (вещественные и комплексные)

$$\dot x = Ax \tag{1}, \quad A\text{ — постоянная }(n\times n)\text{ матрица}, \quad t\in(-\infty,+\infty)$$

### Лемма 1 (аналог метода Эйлера)

Ищем $x = ve^{\lambda t}$, $v\neq 0$. Подставляя: $\lambda v e^{\lambda t} = Ave^{\lambda t} \Rightarrow Av=\lambda v$.

> [!theorem] Лемма 1
> $x=ve^{\lambda t}$ — реш. (1) $\Leftrightarrow$ $\lambda$ — с.з. $A$, $v$ — соответствующий с.в.

**Факт:** $\chi(\lambda)=\det(A-\lambda E)$ — хар. мн-н $A$; с.з. — его корни.

### Случай 1: все с.з. простые и вещественные

$\lambda_1,\ldots,\lambda_n$ — попарно различные с.з., $V_1,\ldots,V_n$ — с.в. (ЛНЗ).

$n$ решений $V_ke^{\lambda_k t}$ — ЛНЗ (т.к. при $t=0$: $V_k$ — ЛНЗ векторы).

$$x_{\text{оо}} = \sum_{k=1}^n c_k V_k e^{\lambda_k t}$$

### Случай 2: простые комплексные с.з.

Если $A$ — вещественная и $\lambda=\alpha+i\beta$ ($\beta\neq 0$) — с.з., то $\bar\lambda$ тоже с.з.

$V$ — с.в. к $\lambda \Rightarrow \bar V$ — с.в. к $\bar\lambda$.

$Ve^{\lambda t}$ — $\mathbb{C}$-решение. Т.к. $A$ вещественная: $\operatorname{Re}(Ve^{\lambda t})$ и $\operatorname{Im}(Ve^{\lambda t})$ — $\mathbb{R}$-решения (лемма об овеществлении).

По формуле Эйлера: $Ve^{(\alpha+i\beta)t} = e^{\alpha t}V(\cos\beta t+i\sin\beta t)$.

Паре $\lambda=\alpha\pm i\beta$ соответствует пара $\mathbb{R}$-решений:
$$\operatorname{Re}(Ve^{\lambda t}) = e^{\alpha t}(\operatorname{Re}V\cos\beta t - \operatorname{Im}V\sin\beta t)$$
$$\operatorname{Im}(Ve^{\lambda t}) = e^{\alpha t}(\operatorname{Re}V\sin\beta t + \operatorname{Im}V\cos\beta t)$$

**Пример §786:** $A=\begin{pmatrix}2&1\\3&4\end{pmatrix}$, $\lambda_1=1$, $\lambda_2=5$.

$V_1=\begin{pmatrix}1\\-1\end{pmatrix}$, $V_2=\begin{pmatrix}1\\3\end{pmatrix}$.

**Ответ:** $x=c_1\begin{pmatrix}1\\-1\end{pmatrix}e^t+c_2\begin{pmatrix}1\\3\end{pmatrix}e^{5t}$.

---

## Билет 26. ЛОС с постоянной матрицей. Метод Эйлера: кратные с.з.

### Случай 3: кратные с.з.

$\lambda_1$ — с.з. $A$, алг. кр. $m$, геом. кр. $s = \dim\ker(A-\lambda_1 E)$.

При $s=m$ (все блоки Жордана простые): $m$ ЛНЗ с.в. $\Rightarrow$ $m$ решений $V_ke^{\lambda_1 t}$.

При $s<m$: нужны **присоединённые векторы** (цепочки Жордана).

$B = A-\lambda_1 E$. Строим цепочку: $BV_1=0$, $BV_2=V_1$, $\ldots$, $BV_m=V_{m-1}$.

Соответствующие $m$ решений:
$$\varphi_k(t) = \left(V_1\frac{t^{k-1}}{(k-1)!} + V_2\frac{t^{k-2}}{(k-2)!} + \ldots + V_k\right)e^{\lambda_1 t}, \quad k=1,\ldots,m$$

Они ЛНЗ (разные степени многочленов).

**Алгоритм:** для кратного с.з. $\lambda$:
1. Находим $s$ ЛНЗ с.в. $V_1^{(j)},\ldots$
2. Для каждого с.в. строим цепочку: $BV_2=V_1$, $BV_3=V_2$, ... до длины $p_j$ (длина $j$-й цепочки)
3. $\sum_j p_j = m$ (суммарно $m$ векторов)

**Пример §811:** $A=\begin{pmatrix}2&1&-1\\2&-1&-2\\-1&1&2\end{pmatrix}$, $\lambda=1$ (а.к.=3, г.к.=1).

Одна цепочка длины 3: $BV_1=0$, $BV_2=V_1$, $BV_3=V_2$.

$$\varphi_1(t) = V_1 e^t, \quad \varphi_2(t) = (V_1 t+V_2)e^t, \quad \varphi_3(t) = \left(V_1\frac{t^2}{2}+V_2 t+V_3\right)e^t$$

---

## Билет 27. Экспонента матрицы. Теорема о сходимости

> [!def] Экспонента матрицы
> $$e^A \overset{\text{def}}{=} \sum_{k=0}^\infty \frac{1}{k!}A^k$$

> [!theorem] Теорема 1 (сходимость)
> Для $\forall$ матрицы $A$ $\exists\,e^A$.

**Доказательство.** Частичная сумма $S_m = \sum_{k=0}^m\frac{1}{k!}A^k$. Покажем, что $S_m$ — фундаментальная:

$$\|S_{m+p}-S_m\| = \left\|\sum_{k=m+1}^{m+p}\frac{1}{k!}A^k\right\| \leq \sum_{k=m+1}^{m+p}\frac{\|A\|^k}{k!}$$

Числовой ряд $\sum_{k=0}^\infty\frac{\|A\|^k}{k!} = e^{\|A\|}$ сходится $\Rightarrow$ его частичные суммы — фундаментальные $\Rightarrow$ $S_m$ — фундаментальная $\Rightarrow$ $\exists\,e^A$. $\blacksquare$

**Свойства:**
1. Блочная форма: $e^{\operatorname{diag}(A,B)} = \operatorname{diag}(e^A, e^B)$
2. Подобие: $A=SBS^{-1} \Rightarrow e^A = Se^BS^{-1}$
3. $e^{A+B}=e^Ae^B$ **только если** $AB=BA$

---

## Билет 28. Экспонента как фундаментальная матрица. Формулы Лиувилля–Остроградского и решения ЗК

> [!theorem] Теорема 2
> $e^{At}$ — **фундаментальная матрица** ЛОС $\dot x=Ax$.

**Доказательство.** Покажем, что $e^{At}$ — невырожденное решение МДУ $\dot X=AX$.

Из ряда: $\dot S_m(t) = AS_{m-1}(t) \xrightarrow{m\to\infty} Ae^{At}$.

По теореме Стокса–Зейделя (равн. сх. $\Rightarrow$ можно дифференцировать): $\frac{d}{dt}(e^{At}) = Ae^{At}$.

Невырожденность: $e^{A\cdot 0} = E$ — невырожд. $\Rightarrow$ столбцы ЛНЗ при $t=0$ $\Rightarrow$ ЛНЗ при всех $t$ (по Т об изоморфизме). $\blacksquare$

### Формула Лиувилля–Остроградского для $e^{At}$

$$\det e^{At} = e^{t\operatorname{tr}A}$$

(из общей ф-лы Л-О: $\det\Phi(t) = \det\Phi(0)\cdot e^{\int_0^t\operatorname{tr}A\,ds} = 1\cdot e^{t\operatorname{tr}A}$)

### Формулы решения ЗК

Для ЛОС $\dot x=Ax$, $x(t_0)=x_0$:
$$\boxed{x(t,t_0,x_0) = e^{A(t-t_0)}x_0}$$

*(т.к. $(e^{At})^{-1} = e^{-At}$, т.к. $e^{At}e^{-At} = e^0 = E$)*

Для ЛНС $\dot x=Ax+f(t)$, $x(t_0)=x_0$:
$$\boxed{x(t,t_0,x_0) = e^{A(t-t_0)}x_0 + e^{At}\int_{t_0}^t e^{-As}f(s)\,ds}$$

---

## Билет 29. Вычисление экспоненты матрицы

### Алгоритм через ЖНФ

$A = SJS^{-1}$, $J=\operatorname{diag}(J_1,\ldots,J_m)$ — блоки Жордана.

$e^{At} = Se^{Jt}S^{-1}$, $e^{Jt} = \operatorname{diag}(e^{J_1t},\ldots,e^{J_mt})$.

> [!theorem] Лемма
> Если $J_s$ — жорданова клетка размера $p$ с с.з. $\lambda_s$:
> $$e^{J_s t} = e^{\lambda_s t}\begin{pmatrix}1 & t & \frac{t^2}{2!} & \cdots & \frac{t^{p-1}}{(p-1)!} \\ 0 & 1 & t & \cdots & \frac{t^{p-2}}{(p-2)!} \\ \vdots & & \ddots & & \vdots \\ 0 & \cdots & 0 & 1 & t \\ 0 & \cdots & & 0 & 1\end{pmatrix}$$

**Доказательство:** $J_s = \lambda_s E_p + I_p$, $\lambda_s E_p$ и $I_p$ коммутируют $\Rightarrow e^{J_s t} = e^{\lambda_s t}e^{I_p t}$.

$I_p$ — нильпотентная ($I_p^p=0$) $\Rightarrow e^{I_p t} = \sum_{k=0}^{p-1}\frac{t^k}{k!}I_p^k$ — конечная сумма. $\blacksquare$

### Замечание о нахождении $S$

\# блоков Жордана = геом. кр. с.з.

Если $J$ не диагональная, $S$ находится из матр. ур-я $AS=SJ$, т.е. из цепочек: $AS_1=\lambda S_1$, $AS_2=\lambda S_2+S_1$, и т.д.

### Пример

$A$ с единственным с.з. $\lambda=1$ а.к.=3, г.к.=1. $J=\begin{pmatrix}1&1&0\\0&1&1\\0&0&1\end{pmatrix}$.

$$e^{At} = Se^{Jt}S^{-1}, \quad e^{Jt} = e^t\begin{pmatrix}1&t&t^2/2\\0&1&t\\0&0&1\end{pmatrix}$$

---

## Билет 30. Теоремы 1 и 2 об оценке экспоненты матрицы

### Теорема 1: $e^{At} \to 0$ при $t\to+\infty$

> [!theorem] Теорема 1
> $e^{At} \xrightarrow{t\to+\infty} 0 \Leftrightarrow \forall\,k=1,\ldots,n\colon \operatorname{Re}\lambda_k < 0$.

**Доказательство.** $e^{At} = Se^{Jt}S^{-1}$, где $J = \operatorname{diag}(J_1,\ldots,J_m)$ — ЖНФ.

$e^{At}\to 0 \Leftrightarrow e^{Jt}\to 0 \Leftrightarrow e^{J_k t}\to 0$ $\forall\,k$.

Элементы $e^{J_k t}$ имеют вид $e^{\lambda_k t}\dfrac{t^r}{r!}$, $0\leq r\leq\dim J_k-1$.

$\left|e^{\lambda_k t}\dfrac{t^r}{r!}\right| = e^{\operatorname{Re}\lambda_k\cdot t}\cdot\dfrac{t^r}{r!} \to 0 \Leftrightarrow \operatorname{Re}\lambda_k < 0$

(т.к. при $\operatorname{Re}\lambda_k<0$ экспонента убивает любую полиномиальную степень $t^r$). $\blacksquare$

### Теорема 2: $\|e^{At}\| < +\infty$ при $t\geq 0$

> [!theorem] Теорема 2
> $\|e^{At}\| < +\infty$ при $t\geq 0 \Leftrightarrow \forall\,k\colon \operatorname{Re}\lambda_k\leq 0$, причём с.з. с $\operatorname{Re}\lambda_k=0$ отвечают только простым (однородным) блокам Жордана (г.кр. = а.кр., т.е. $\dim J_k = 1$).

**Доказательство.** Элемент $e^{\lambda_k t}\dfrac{t^r}{r!}$ ограничен при $t\geq 0$:
- $\operatorname{Re}\lambda_k > 0$: $e^{\operatorname{Re}\lambda_k t}\to+\infty$ — не ограничено ❌
- $\operatorname{Re}\lambda_k < 0$: стремится к $0$ — ограничено ✓
- $\operatorname{Re}\lambda_k = 0$: $|e^{\lambda_k t}| = 1$, поэтому $\dfrac{t^r}{r!}$ — ограничено $\Leftrightarrow r=0$ $\Leftrightarrow$ $\dim J_k=1$. $\blacksquare$

---

## Билет 31. Устойчивость по Ляпунову. Определения

Рассматриваем НСДУ: $\dot x = f(t,x)$ **(1)**, $f\in C(G)$, $f\in\mathrm{Lip}_x^{\mathrm{loc}}(G)$.

Пусть $x=\varphi(t)$ — произвольное решение (1), определённое на $[t_0,+\infty)$.

> [!def] Устойчивость по Ляпунову (У)
> Решение $\varphi(t)$ называется **устойчивым по Ляпунову**, если:
> $$\forall\,\varepsilon>0\;\exists\,\delta=\delta(\varepsilon)>0\colon \forall\text{ реш.}\,\Psi(t)\colon \|\Psi(t_0)-\varphi(t_0)\|<\delta \Rightarrow \|\Psi(t)-\varphi(t)\|<\varepsilon\;\forall\,t\geq t_0$$
>
> (малое начальное отклонение $\Rightarrow$ малое отклонение при всех $t\geq t_0$)

> [!def] Равномерная устойчивость
> Если $\delta$ не зависит от $t_0$, то решение **равномерно устойчиво**.

> [!def] Асимптотическая устойчивость (АУ)
> Решение $\varphi(t)$ называется **АУ**, если оно У и дополнительно:
> $$\exists\,\Delta>0\colon \|\Psi(t_0)-\varphi(t_0)\|<\Delta \Rightarrow \|\Psi(t)-\varphi(t)\|\xrightarrow{t\to+\infty} 0$$
>
> ($\varphi$ — аттрактор: соседние решения притягиваются к нему)

> [!def] Неустойчивость (НУ)
> Решение **неустойчиво**, если $\exists\,\varepsilon>0$: $\forall\,\delta>0$ $\exists\,\Psi(t)$ с $\|\Psi(t_0)-\varphi(t_0)\|<\delta$ и $\exists\,t^*$: $\|\Psi(t^*)-\varphi(t^*)\|\geq\varepsilon$.

**Соотношение:** У, АУ, НУ — дизъюнктивны; $\Pi\not\Rightarrow AY$ (маятник без трения — У, но не АУ).

**Пример:** математический маятник $\ddot x+\sin x=0$. $x=0$ — У; $x=\pi$ — НУ. С трением $\ddot x+k\dot x+\sin x=0$: $x=0$ — АУ.

---

## Билет 32. Устойчивость линейных систем. Сведение к ЛОС. Свойство стадности

### Сведение к нулевому решению

$\Delta$ ЛНС: $\dot x = A(t)x + f(t)$ **(1)**, $\Delta$ ЛОС: $\dot y = A(t)y$ **(2)**.

> [!theorem] Теорема 1
> Тип устойчивости любого решения $\varphi(t)$ ЛНС (1) совпадает с типом устойчивости нулевого решения $y=0$ ЛОС (2).

**Доказательство.** Замена $y = x-\varphi(t)$ — параллельный перенос, переводящий $\varphi$ в $0$. Не меняет расстояния между решениями $\Rightarrow$ сохраняет тип устойчивости.

$$\dot y = \dot x - \dot\varphi = (A(t)x+f(t)) - (A(t)\varphi+f(t)) = A(t)(x-\varphi) = A(t)y$$

ЛНС (1) $\to$ ЛОС (2). Тип устойчивости сохраняется. $\blacksquare$

**Замечание:** тип устойчивости зависит **только от $A(t)$** и не зависит от $f(t)$.

### Свойство стадности

**Следствие (стадность):** У ЛНС (1) все решения однородно либо У, либо АУ, либо НУ. Позволяет говорить о **У/АУ/НУ ЛОС** (а не отдельного решения).

**Предостережение:** нельзя смешивать У ЛОС и У ЛНС. Пример: $\dot x=-x+t$. ЛОУ $\dot x=-x$ — АУ, но общее решение $x=ce^{-t}+t-1\to+\infty$ при $t\to+\infty$ — НУ.

---

## Билет 33. Теорема об устойчивости ЛОС

> [!theorem] Теорема 2 (об У ЛОС)
> Следующие условия эквивалентны:
> 1. ЛОС — **У**
> 2. Все решения ЛОС ограничены на $[t_0,+\infty)$
> 3. $\exists$ ограниченная ФСР $\varphi_1(t),\ldots,\varphi_n(t)$ на $[t_0,+\infty)$
> 4. $\exists$ ограниченная ФМ $\Phi(t)$ на $[t_0,+\infty)$

**Доказательство (У $\Rightarrow$ все реш. огр.):** $\exists\,\varepsilon=1$, $\exists\,\delta=\delta(1)$.

Берём реш. $\Psi(t)$ и $\Delta$ реш. $y(t) = \dfrac{\delta}{2\|\Psi(t_0)\|}\Psi(t)$.

$\|y(t_0)\| = \dfrac{\delta}{2} < \delta \Rightarrow \|y(t)\| < 1$ $\forall\,t\geq t_0$.

$\|\Psi(t)\| = \dfrac{2\|\Psi(t_0)\|}{\delta}\|y(t)\| < \dfrac{2\|\Psi(t_0)\|}{\delta}$ — ограничено.

**Доказательство (огр. ФМ $\Rightarrow$ У):** $\|\Phi(t)\|\leq M$. По ф-ле $y(t)=\Phi(t)\Phi^{-1}(t_0)y(t_0)$:

$$\|y(t)\|\leq M\|\Phi^{-1}(t_0)\|\|y(t_0)\|$$

Берём $\delta=\dfrac{\varepsilon}{M\|\Phi^{-1}(t_0)\|}$: если $\|y(t_0)\|<\delta$, то $\|y(t)\|<\varepsilon$. $\blacksquare$

---

## Билет 34. Теорема об асимптотической устойчивости ЛОС

> [!theorem] Теорема 3 (об АУ ЛОС)
> Следующие условия эквивалентны:
> 1. ЛОС — **АУ**
> 2. Все решения ЛОС $\to 0$ при $t\to+\infty$
> 3. $\exists$ ФСР $\varphi_1(t),\ldots,\varphi_n(t)$ с $\varphi_k(t)\to 0$ при $t\to+\infty$
> 4. $\exists$ ФМ $\Phi(t)\to 0$ при $t\to+\infty$

**Доказательство (4 $\Rightarrow$ 2):** $\forall$ реш. $y(t)=\Phi(t)c$, $c\in\mathbb{R}^n$: $\|y(t)\|\leq\|\Phi(t)\|\|c\|\to 0$.

**Доказательство (1 $\Rightarrow$ 2):** из АУ $\exists\,\Delta>0$. Берём $y(t_0)=y_0$ с $\|y_0\|<\Delta$: $y(t)=\Phi(t)\Phi^{-1}(t_0)y_0$. $\Phi(t)\to 0 \Rightarrow y(t)\to 0$. $\blacksquare$

---

## Билет 35. Устойчивость ЛОС с постоянной матрицей

$$\dot x = Ax, \quad A\text{ — постоянная}$$

$\Phi(t) = e^{At}$ — ФМ.

> [!theorem] Теорема 4
> 1. **АУ** $\Leftrightarrow$ $\forall\,k\colon\operatorname{Re}\lambda_k < 0$
> 2. **У** $\Leftrightarrow$ $\forall\,k\colon\operatorname{Re}\lambda_k\leq 0$, причём с.з. с $\operatorname{Re}\lambda_k=0$ — простые (г.кр. = а.кр.)

**Доказательство:**
1. $e^{At}\to 0$ при $t\to+\infty \Leftrightarrow \operatorname{Re}\lambda_k<0$ $\forall\,k$ (по Т1 §10) $\Leftrightarrow$ АУ (по Т3).
2. $\|e^{At}\|<+\infty$ при $t\geq 0 \Leftrightarrow$ усл. на с.з. (по Т2 §10) $\Leftrightarrow$ У (по Т2). $\blacksquare$

**Аналог для ЛОУ с пост. коэф.:** хар. мн-н $\chi(\lambda)$, корни $\lambda_1,\ldots,\lambda_n$:
- АУ $\Leftrightarrow$ $\operatorname{Re}\lambda_k<0$ $\forall\,k$
- У $\Leftrightarrow$ $\operatorname{Re}\lambda_k\leq 0$ $\forall\,k$ и при $\operatorname{Re}\lambda_k=0$: $\lambda_k$ — простой корень $\chi$

---

## Билет 36. Устойчивость по первому приближению

$\Delta$ НСДУ: $\dot x = f(t,x)$ **(1)**, $f\in C(G)$, $f\in\mathrm{Lip}_x^{\mathrm{loc}}$.

$\varphi(t)$ — решение, исследуем на У. Замена $y=x-\varphi(t)$:
$$\dot y = f(t,y+\varphi(t)) - f(t,\varphi(t)) = A(t)y + g(t,y)$$

где $A(t) = f'_x(t,\varphi(t))$ — матрица Якоби, $\|g(t,y)\|=o(\|y\|)$ при $y\to 0$.

**Система 1-го приближения:** $\dot y = A(t)y$ **(3)**.

Частный случай: $A(t)\equiv A$ постоянная, $\lambda_1,\ldots,\lambda_n$ — с.з. $A$.

> [!theorem] Теорема Ляпунова (об устойчивости по 1-му приближению)
> Пусть $\|g(t,y)\|\leq\alpha(\|y\|)\|y\|$, $\alpha(y)\to 0$ при $y\to 0$. Тогда:
> 1. Если (3) — **АУ** (т.е. $\operatorname{Re}\lambda_k<0$ $\forall\,k$) $\Rightarrow$ $y=0$ в (2) — **АУ** (т.е. $\varphi(t)$ — АУ в (1))
> 2. Если $\exists\,k\colon\operatorname{Re}\lambda_k>0$ (сильно НУ) $\Rightarrow$ $\varphi(t)$ — **НУ** в (1)

*(без доказательства)*

**Критический случай** ($\operatorname{Re}\lambda_k=0$ для некоторых): тип устойчивости определяется нелинейными членами — нужен метод Ляпунова.

**Схема:**
$$\text{линеаризация АУ} \Rightarrow \text{нелинейная АУ}$$
$$\text{линеаризация сильно НУ} \Rightarrow \text{нелинейная НУ}$$

---

## Билет 37. Линейные разностные уравнения $n$-го порядка. Хар. многочлен. ФСР. Формула общего решения

### ЛОРУ

$$a_n x_{k+n} + a_{n-1}x_{k+n-1} + \ldots + a_1 x_{k+1} + a_0 x_k = 0 \tag{1}$$

$a_k\in\mathbb{R}$, $a_n\neq 0$, $a_0\neq 0$ (иначе порядок меньше $n$).

$M^\circ$ — мн-во всех решений (1).

> [!theorem] Лемма (линейность $M^\circ$)
> $M^\circ$ — линейное пространство: $L(c_1\{x_k\}+c_2\{\bar x_k\}) = c_1 L(\{x_k\})+c_2 L(\{\bar x_k\}) = 0$.

> [!theorem] Теорема об изоморфизме
> $M^\circ\cong\mathbb{R}^n$: изоморфизм $h(\{x_k\}) = \begin{pmatrix}x_{n-1}\\\vdots\\x_0\end{pmatrix}$.
>
> Следствие: $\dim M^\circ = n$.

> [!def] ФСР
> $n$ ЛНЗ решений $\{x_k^{(1)}\},\ldots,\{x_k^{(n)}\}$ называются **фундаментальной системой решений (ФСР)** ЛОРУ.

**Критерий ЛНЗ:** решения ЛНЗ $\Leftrightarrow$ начальные векторы $\begin{pmatrix}x_{n-1}^{(j)}\\\vdots\\x_0^{(j)}\end{pmatrix}$ ЛНЗ в $\mathbb{R}^n$.

**Формула общего решения:**
$$x_k = c_1 x_k^{(1)} + c_2 x_k^{(2)} + \ldots + c_n x_k^{(n)}, \quad c_j\in\mathbb{R}$$

> [!def] Характеристический многочлен
> $\chi(\lambda) = a_n\lambda^n + a_{n-1}\lambda^{n-1} + \ldots + a_1\lambda + a_0$ — **хар. мн-н** ЛОРУ.

**Ключевой факт:** $\lambda^k$ — реш. ЛОРУ $\Leftrightarrow$ $\chi(\lambda)=0$.

---

## Билет 38. Метод Эйлера построения ФСР ЛОРУ: простые вещественные, комплексные и кратные корни

### Случай 1: все корни вещественные и простые

$\lambda_1,\ldots,\lambda_n$ — попарно различные корни $\chi$, все $\in\mathbb{R}$.

Решения: $\{\lambda_j^k\}$, $j=1,\ldots,n$.

ЛНЗ: определитель Ван-дер-Монда $= \prod_{i<j}(\lambda_j-\lambda_i)\neq 0$. $\Rightx$ это ФСР.

$$x_k = \sum_{j=1}^n c_j\lambda_j^k$$

### Случай 2: простые комплексные корни

$\lambda_1=\alpha+i\beta = re^{i\varphi}$ ($\beta\neq 0$, $r=\sqrt{\alpha^2+\beta^2}$, $\varphi=\arg\lambda_1$) — корень $\chi\in\mathbb{R}[\lambda]$ $\Rightarrow$ $\bar\lambda_1=re^{-i\varphi}$ тоже корень.

$(\lambda_1)^k = r^k(\cos k\varphi + i\sin k\varphi)$ — $\mathbb{C}$-решение.

Пара $\mathbb{R}$-решений:
$$\operatorname{Re}(\lambda_1^k) = r^k\cos k\varphi, \qquad \operatorname{Im}(\lambda_1^k) = r^k\sin k\varphi$$

**Пример:** $x_{k+2}+x_k=0$. $\lambda^2+1=0$, $\lambda_{1,2}=\pm i=e^{\pm i\pi/2}$, $r=1$, $\varphi=\pi/2$.

$x_k = c_1\cos\dfrac{\pi k}{2} + c_2\sin\dfrac{\pi k}{2}$.

### Случай 3: кратный корень $\lambda_0$ кратности $m$

> [!theorem] Лемма 2
> $\lambda_0^k,\; k\lambda_0^k,\; k^2\lambda_0^k,\; \ldots,\; k^{m-1}\lambda_0^k$ — $m$ решений ЛОРУ.

**Доказательство (для $m=2$):** $\chi(\lambda_0)=0$, $\chi'(\lambda_0)=0$.

$L(k\lambda_0^k) = \lambda_0^k(k\chi(\lambda_0)+\lambda_0\chi'(\lambda_0)) = \lambda_0^k(0+0)=0$. $\blacksquare$

**Кратные комплексные корни:** если $\lambda=re^{i\varphi}$ — кр. $m$, то $2m$ вещественных решений:
$$r^k\cos k\varphi,\; r^k\sin k\varphi,\; kr^k\cos k\varphi,\; kr^k\sin k\varphi,\; \ldots,\; k^{m-1}r^k\cos k\varphi,\; k^{m-1}r^k\sin k\varphi$$

**Пример 1:** $x_{k+3}-x_{k+2}-x_{k+1}+x_k=0$.

$\chi(\lambda)=(\lambda-1)^2(\lambda+1)$. $\lambda_1=1$ кр.2: $1, k$. $\lambda_2=-1$ кр.1: $(-1)^k$.

$x_k = c_1 + c_2 k + c_3(-1)^k$.

**Пример 2:** $x_{k+4}+8x_{k+2}+16x_k=0$.

$\chi(\lambda)=(\lambda^2+4)^2=(\lambda-2i)^2(\lambda+2i)^2$. $r=2$, $\varphi=\pi/2$, кр.2.

$x_k = 2^k[(c_1+c_2 k)\cos\tfrac{\pi k}{2}+(c_3+c_4 k)\sin\tfrac{\pi k}{2}]$.

**Числа Фибоначчи:** $F_{k+2}=F_{k+1}+F_k$, $F_0=F_1=1$.

$\chi(\lambda)=\lambda^2-\lambda-1=0$, $\lambda_{1,2}=\dfrac{1\pm\sqrt{5}}{2}$.

$$F_k = \dfrac{1}{\sqrt{5}}\left[\left(\dfrac{1+\sqrt{5}}{2}\right)^{k+1}-\left(\dfrac{1-\sqrt{5}}{2}\right)^{k+1}\right] \quad \text{(формула Бине)}$$

---

## Билет 42. Линейное неоднородное разностное уравнение. Теорема о множестве решений. МНК

### ЛНРУ

$$a_n x_{k+n} + \ldots + a_0 x_k = f_k \tag{1}, \quad a_n,a_0\neq 0$$

> [!theorem] Лемма 1
> Разность двух решений ЛНРУ есть решение ЛОРУ.

**Доказательство:** $L(\bar x_k - \tilde x_k) = f_k - f_k = 0$. $\blacksquare$

> [!theorem] Теорема 1 (о множестве решений ЛНРУ)
> Пусть $x_k^\circ$ — общее решение ЛОРУ, $x_k^{\text{ч}}$ — одно частное решение ЛНРУ. Тогда:
> $$x_k^{\text{оо}} = x_k^\circ + x_k^{\text{ч}} = \sum_{j=1}^n c_j x_k^{(j)} + x_k^{\text{ч}}$$
> — общее решение ЛНРУ.

### МНК для ЛНРУ

**Правая часть специального вида:**
$$f_k = \rho^k(P(k)\cos k\theta + Q(k)\sin k\theta)$$

**Контрольное число:** $\sigma = \rho e^{i\theta} = \rho(\cos\theta+i\sin\theta)$.

$$s = \begin{cases}0, & \sigma\text{ — не корень }\chi\\m, & \sigma\text{ — корень }\chi\text{ кратности }m\end{cases}$$

> [!theorem] Теорема 2 (МНК)
> ЛНРУ имеет единственное частное решение вида:
> $$x_k^{\text{ч}} = k^s\rho^k(\tilde P(k)\cos k\theta + \tilde Q(k)\sin k\theta)$$
> где $\deg\tilde P = \deg\tilde Q = \max\{\deg P, \deg Q\}$. Многочлены $\tilde P, \tilde Q$ находятся МНК.

### Пример 1: $x_{k+2}+3x_{k+1}+2x_k=(20k+7)\cdot 3^k$

ЛОРУ: $\lambda_{1,2}=-1,-2$. $x_k^\circ=c_1(-1)^k+c_2(-2)^k$.

МНК: $\sigma=3$, $\chi(3)=9+9+2=20\neq 0 \Rightarrow s=0$.

$x_k^{\text{ч}}=(ak+b)\cdot 3^k$. Подстановка даёт $a=1$, $b=-1$.

**Ответ:** $x_k=c_1(-1)^k+c_2(-2)^k+(k-1)\cdot 3^k$.

### Пример 2: вычисление $S=\sum_{m=0}^\infty\dfrac{m^2}{2^m}$

$S_k-S_{k-1}=\dfrac{k^2}{2^k}$ — ЛНРУ. $\sigma=\dfrac{1}{2}$ — не корень $\chi$ (ЛОРУ: $S_k-S_{k-1}=0$, $\lambda=1$).

$S_k^{\text{ч}}=\left(\dfrac{1}{2}\right)^k(ak^2+bk+c)$. Подстановка: $a=-1$, $b=-4$, $c=-6$.

$S_k=C-\dfrac{k^2+4k+6}{2^k}$. Из $S_1=\dfrac{1}{2}$: $C=6$.

$$S = \lim_{k\to\infty}S_k = 6$$

### Принцип суммы

Если $x_k^{(1)}$ — реш. $L(x_k)=f_k^{(1)}$ и $x_k^{(2)}$ — реш. $L(x_k)=f_k^{(2)}$, то $x_k^{(1)}+x_k^{(2)}$ — реш. $L(x_k)=f_k^{(1)}+f_k^{(2)}$.